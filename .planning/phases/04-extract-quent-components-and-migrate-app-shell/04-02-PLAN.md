---
phase: 04-extract-quent-components-and-migrate-app-shell
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - ui/packages/@quent/components/src/ui/button.tsx
  - ui/packages/@quent/components/src/ui/card.tsx
  - ui/packages/@quent/components/src/ui/collapsible.tsx
  - ui/packages/@quent/components/src/ui/data-text.tsx
  - ui/packages/@quent/components/src/ui/dropdown-menu.tsx
  - ui/packages/@quent/components/src/ui/hover-card.tsx
  - ui/packages/@quent/components/src/ui/input.tsx
  - ui/packages/@quent/components/src/ui/navigation-menu.tsx
  - ui/packages/@quent/components/src/ui/popover.tsx
  - ui/packages/@quent/components/src/ui/resizable.tsx
  - ui/packages/@quent/components/src/ui/scroll-area.tsx
  - ui/packages/@quent/components/src/ui/select-field.tsx
  - ui/packages/@quent/components/src/ui/select.tsx
  - ui/packages/@quent/components/src/ui/skeleton.tsx
  - ui/packages/@quent/components/src/ui/tree-table.tsx
  - ui/packages/@quent/components/src/ui/tree-view.tsx
  - ui/packages/@quent/components/src/lib/echarts.ts
  - ui/packages/@quent/components/src/lib/timeline.utils.ts
  - ui/packages/@quent/components/src/lib/resource.utils.ts
  - ui/packages/@quent/components/src/lib/queryBundle.utils.ts
  - ui/packages/@quent/components/src/services/query-plan/dagFieldProcessing.ts
  - ui/packages/@quent/components/src/services/query-plan/query-bundle-transformer.ts
  - ui/packages/@quent/components/src/services/query-plan/operationTypes.ts
  - ui/packages/@quent/components/src/services/query-plan/types.ts
  - ui/packages/@quent/components/src/dag/DAGChart.tsx
  - ui/packages/@quent/components/src/dag/DAGControls.tsx
  - ui/packages/@quent/components/src/dag/DAGLegend.tsx
  - ui/packages/@quent/components/src/dag/DAGSettingsPopover.tsx
  - ui/packages/@quent/components/src/timeline/Timeline.tsx
  - ui/packages/@quent/components/src/timeline/TimelineController.tsx
  - ui/packages/@quent/components/src/timeline/TimelineSkeleton.tsx
  - ui/packages/@quent/components/src/timeline/TimelineToolbar.tsx
  - ui/packages/@quent/components/src/timeline/TimelineTooltip.tsx
  - ui/packages/@quent/components/src/timeline/ResourceTimeline.tsx
  - ui/packages/@quent/components/src/timeline/useTimelineChartColors.ts
  - ui/packages/@quent/components/src/timeline/types.ts
  - ui/packages/@quent/components/src/query-plan/QueryPlanNode.tsx
  - ui/packages/@quent/components/src/query-plan/OperatorStatisticsPopup.tsx
  - ui/packages/@quent/components/src/resource-tree/ResourceColumn.tsx
  - ui/packages/@quent/components/src/resource-tree/ResourceGroupRow.tsx
  - ui/packages/@quent/components/src/resource-tree/ResourceRow.tsx
  - ui/packages/@quent/components/src/resource-tree/InlineSelector.tsx
  - ui/packages/@quent/components/src/resource-tree/UsageColumn.tsx
  - ui/packages/@quent/components/src/resource-tree/types.ts
  - ui/packages/@quent/components/src/operator-timeline/index.ts
  - ui/packages/@quent/components/src/operator-timeline/OperatorGanttChart.tsx
  - ui/packages/@quent/components/src/operator-timeline/types.ts
  - ui/packages/@quent/components/src/operator-timeline/utils.ts
  - ui/packages/@quent/components/src/index.ts
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05
  - COMP-06
  - COMP-07

must_haves:
  truths:
    - "All 16 UI primitives importable from @quent/components"
    - "DAGChart importable from @quent/components with controlled-first API (selectedNodeIds + onSelectionChange props)"
    - "TimelineController and all timeline components importable from @quent/components"
    - "No component in @quent/components imports from @/contexts/ThemeContext"
    - "Every exported component has JSDoc comment"
    - "Every exported component has its props interface exported"
    - "index.ts uses only named exports (no export *)"
  artifacts:
    - path: "ui/packages/@quent/components/src/index.ts"
      provides: "Complete barrel export for @quent/components"
      min_lines: 50
    - path: "ui/packages/@quent/components/src/dag/DAGChart.tsx"
      provides: "DAGChart with controlled-first API"
      contains: "selectedNodeIds"
    - path: "ui/packages/@quent/components/src/timeline/useTimelineChartColors.ts"
      provides: "useTimelineChartColors accepting isDark parameter"
      contains: "isDark: boolean"
  key_links:
    - from: "ui/packages/@quent/components/src/dag/DAGChart.tsx"
      to: "@quent/hooks"
      via: "import { useSelectedNodeIds, useSetSelectedNodeIds } from '@quent/hooks'"
      pattern: "from '@quent/hooks'"
    - from: "ui/packages/@quent/components/src/timeline/Timeline.tsx"
      to: "ui/packages/@quent/components/src/lib/echarts.ts"
      via: "relative import"
      pattern: "from '../lib/echarts'"
    - from: "ui/packages/@quent/components/src/index.ts"
      to: "all component modules"
      via: "named re-exports"
      pattern: "export \\{ .+ \\} from"
---

<objective>
Copy all UI components, internal utilities, and service files into @quent/components. Refactor ThemeContext coupling to isDark props. Add controlled-first API to DAGChart. Build the barrel export.

Purpose: This is the core extraction — after this plan, `@quent/components` is a complete, self-contained component library. Components use relative imports internally and `@quent/utils`/`@quent/hooks` for external deps. No `@/` path aliases exist in the package.

Output: ~50 files in @quent/components with a complete `index.ts` barrel. The app still works via the original files (not yet migrated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-extract-quent-components-and-migrate-app-shell/04-CONTEXT.md
@.planning/phases/04-extract-quent-components-and-migrate-app-shell/04-RESEARCH.md
@.planning/phases/04-extract-quent-components-and-migrate-app-shell/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: new @quent/utils exports -->
```typescript
// Entity types
export { EntityTypeKey } from './entityTypes';
export type { EntityTypeValue, SingleEntity, EntityRefKey } from './entityTypes';
// DAG types
export { NODE_LABEL_FIELD } from './dagTypes';
export type { DAGNode, DAGEdge, StatValue, NodeColoring, EdgeColoring, EdgeWidthConfig, NodeLabelField, ... } from './dagTypes';
```

<!-- From Plan 01: new @quent/hooks exports -->
```typescript
export { useDagNodeColoring, useDagEdgeWidthConfig, useDagEdgeColoring, useOperatorStatFields, usePortStatFields } from './dag/useDagControls';
export { useNodeColoring } from './dag/useNodeColoring';
export { useSelectedColorField, useNodeColoringValue, useNodeColorPalette, useSelectedEdgeWidthField, useEdgeWidthConfig, useSelectedEdgeColorField, useEdgeColoring, useEdgeColorPalette, useSelectedNodeLabelField } from './dag/dagControlSelectors';
export { useDeferredReady } from './dag/useDeferredReady';
```

<!-- Existing @quent/hooks exports (from Phase 3) -->
```typescript
export { useSelectedNodeIds, useSetSelectedNodeIds } from './dag/useSelectedNodeIds';
export { useSelectedOperatorLabel, useSetSelectedOperatorLabel } from './dag/useSelectedOperatorLabel';
export { useSelectedPlanId, useSetSelectedPlanId } from './dag/useSelectedPlanId';
export { useHoveredWorkerId, useSetHoveredWorkerId } from './dag/useHoveredWorkerId';
export { useTimelineData, useIsTimelineHovered, useZoomRange, useSetZoomRange, ... } from './timeline/useTimelineAtoms';
export { timelineCacheKey } from './atoms/timeline';
export { useBulkTimelines } from './timeline/useBulkTimelines';
export { useBulkTimelineFetch, applyBulkTimelineResponse, buildMergedBulkEntries } from './timeline/useBulkTimelineFetch';
export { useHighlightedItemIds } from './timeline/useHighlightedItemIds';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy UI primitives and internal utility files into @quent/components</name>
  <files>
    ui/packages/@quent/components/src/ui/ (16 files)
    ui/packages/@quent/components/src/lib/echarts.ts
    ui/packages/@quent/components/src/lib/timeline.utils.ts
    ui/packages/@quent/components/src/lib/resource.utils.ts
    ui/packages/@quent/components/src/lib/queryBundle.utils.ts
    ui/packages/@quent/components/src/services/query-plan/dagFieldProcessing.ts
    ui/packages/@quent/components/src/services/query-plan/query-bundle-transformer.ts
    ui/packages/@quent/components/src/services/query-plan/operationTypes.ts
    ui/packages/@quent/components/src/services/query-plan/types.ts
  </files>
  <read_first>
    ui/src/components/ui/button.tsx
    ui/src/components/ui/card.tsx
    ui/src/components/ui/collapsible.tsx
    ui/src/components/ui/data-text.tsx
    ui/src/components/ui/dropdown-menu.tsx
    ui/src/components/ui/hover-card.tsx
    ui/src/components/ui/input.tsx
    ui/src/components/ui/navigation-menu.tsx
    ui/src/components/ui/popover.tsx
    ui/src/components/ui/resizable.tsx
    ui/src/components/ui/scroll-area.tsx
    ui/src/components/ui/select-field.tsx
    ui/src/components/ui/select.tsx
    ui/src/components/ui/skeleton.tsx
    ui/src/components/ui/tree-table.tsx
    ui/src/components/ui/tree-view.tsx
    ui/src/lib/echarts.ts
    ui/src/lib/timeline.utils.ts
    ui/src/lib/resource.utils.ts
    ui/src/lib/queryBundle.utils.ts
    ui/src/services/query-plan/dagFieldProcessing.ts
    ui/src/services/query-plan/query-bundle-transformer.ts
    ui/src/services/query-plan/operationTypes.ts
    ui/src/services/query-plan/types.ts
  </read_first>
  <action>
    **Copy all 16 UI primitive components** from `ui/src/components/ui/` to `ui/packages/@quent/components/src/ui/`:
    - button.tsx, card.tsx, collapsible.tsx, data-text.tsx, dropdown-menu.tsx, hover-card.tsx, input.tsx, navigation-menu.tsx, popover.tsx, resizable.tsx, scroll-area.tsx, select-field.tsx, select.tsx, skeleton.tsx, tree-table.tsx, tree-view.tsx

    For each primitive file, update imports:
    - `from '@/lib/utils'` or `from '@/lib/cn'` → already uses `from '@quent/utils'` (verified: primitives already migrated in Phase 2). If any still use `@/lib/utils`, change to `import { cn } from '@quent/utils';`
    - `from '@/components/ui/X'` → `from './X'` (relative within ui/ directory)
    - Verify each component accepts `className?: string` prop and applies via `cn()` at root element (per COMP-06). If any component is missing this, add it.
    - Add JSDoc comment to each exported component if missing (per COMP-04): `/** Brief description. */`
    - Ensure props interface is exported alongside the component (per COMP-05): e.g. `export interface ButtonProps { ... }`

    **Copy internal utility files** per D-05 (these are NOT exported from index.ts):

    `ui/src/lib/echarts.ts` → `ui/packages/@quent/components/src/lib/echarts.ts`:
    - No import changes needed — it only imports from `echarts` package.

    `ui/src/lib/timeline.utils.ts` → `ui/packages/@quent/components/src/lib/timeline.utils.ts`:
    - Update `import type { ZoomRange } from '@quent/utils';` (should already be correct).
    - Update any `@/` imports to relative paths or `@quent/*` package imports.

    `ui/src/lib/resource.utils.ts` → `ui/packages/@quent/components/src/lib/resource.utils.ts`:
    - `import { EntityTypeKey } from '@/types'` → `import { EntityTypeKey } from '@quent/utils';`
    - `import type { TreeTableItem } from '@/components/resource-tree/types'` → `import type { TreeTableItem } from '../resource-tree/types';`
    - Update any other `@/` imports similarly.

    `ui/src/lib/queryBundle.utils.ts` → `ui/packages/@quent/components/src/lib/queryBundle.utils.ts`:
    - `import type { EntityRefKey } from '@/types'` → `import type { EntityRefKey } from '@quent/utils';`
    - Update any other `@/` imports.

    **Copy service files** per D-05:

    `ui/src/services/query-plan/types.ts` → `ui/packages/@quent/components/src/services/query-plan/types.ts`:
    - This file was updated in Plan 01 to re-export coloring types from `@quent/utils`. Keep that.
    - `import type { TreeDataItem } from '@/components/ui/tree-view'` → `import type { TreeDataItem } from '../../ui/tree-view';`
    - Keep the remaining type definitions (DAGNode, DAGEdge, etc.) — BUT if Plan 01 moved DAGNode/DAGEdge/StatValue to @quent/utils, then this file should re-export from @quent/utils:
      ```typescript
      export type { DAGNode, DAGEdge, StatValue, NodeColoring, EdgeColoring, EdgeWidthConfig, ... } from '@quent/utils';
      ```
      Plus keep the types NOT moved (QueryPlanDataItem, DAGData, QueryPlanNodeData) defined locally with correct relative imports.

    `ui/src/services/query-plan/dagFieldProcessing.ts` → `ui/packages/@quent/components/src/services/query-plan/dagFieldProcessing.ts`:
    - Update imports: `@/services/query-plan/types` → `./types` (relative)
    - Update any `@quent/utils` imports (should be fine)
    - Update any `@/` imports to relative paths

    `ui/src/services/query-plan/query-bundle-transformer.ts` → `ui/packages/@quent/components/src/services/query-plan/query-bundle-transformer.ts`:
    - `@/services/query-plan/types` → `./types`
    - Any other `@/` → relative or `@quent/*`

    `ui/src/services/query-plan/operationTypes.ts` → `ui/packages/@quent/components/src/services/query-plan/operationTypes.ts`:
    - Update imports as above

    **CRITICAL**: Every `@/` import in copied files must become either:
    - Relative path (for files within @quent/components)
    - `@quent/utils` (for types and utilities)
    - `@quent/hooks` (for hooks)
    No `@/` or `~quent/types` imports should remain in any file in the package.
  </action>
  <verify>
    <automated>cd /Users/johallaron/Projects/quent/ui && npx tsc --noEmit 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - ui/packages/@quent/components/src/ui/button.tsx exists
    - ui/packages/@quent/components/src/ui/tree-view.tsx exists
    - All 16 files exist in ui/packages/@quent/components/src/ui/
    - ui/packages/@quent/components/src/lib/echarts.ts exists
    - ui/packages/@quent/components/src/lib/timeline.utils.ts exists
    - ui/packages/@quent/components/src/lib/resource.utils.ts exists
    - ui/packages/@quent/components/src/lib/queryBundle.utils.ts exists
    - ui/packages/@quent/components/src/services/query-plan/types.ts exists
    - ui/packages/@quent/components/src/services/query-plan/dagFieldProcessing.ts exists
    - ui/packages/@quent/components/src/services/query-plan/query-bundle-transformer.ts exists
    - ui/packages/@quent/components/src/services/query-plan/operationTypes.ts exists
    - `grep -r "from '@/" ui/packages/@quent/components/src/` returns zero results (no @/ imports in package)
    - `grep -r "from '~quent/types" ui/packages/@quent/components/src/` returns zero results
    - `tsc --noEmit` passes from ui/ root
  </acceptance_criteria>
  <done>All 16 UI primitives and all internal utility/service files are in @quent/components with correct imports. No @/ path aliases in the package.</done>
</task>

<task type="auto">
  <name>Task 2: Copy complex components into @quent/components with ThemeContext decoupling and DAGChart controlled API</name>
  <files>
    ui/packages/@quent/components/src/dag/DAGChart.tsx
    ui/packages/@quent/components/src/dag/DAGControls.tsx
    ui/packages/@quent/components/src/dag/DAGLegend.tsx
    ui/packages/@quent/components/src/dag/DAGSettingsPopover.tsx
    ui/packages/@quent/components/src/timeline/Timeline.tsx
    ui/packages/@quent/components/src/timeline/TimelineController.tsx
    ui/packages/@quent/components/src/timeline/TimelineSkeleton.tsx
    ui/packages/@quent/components/src/timeline/TimelineToolbar.tsx
    ui/packages/@quent/components/src/timeline/TimelineTooltip.tsx
    ui/packages/@quent/components/src/timeline/ResourceTimeline.tsx
    ui/packages/@quent/components/src/timeline/useTimelineChartColors.ts
    ui/packages/@quent/components/src/timeline/types.ts
    ui/packages/@quent/components/src/query-plan/QueryPlanNode.tsx
    ui/packages/@quent/components/src/query-plan/OperatorStatisticsPopup.tsx
    ui/packages/@quent/components/src/resource-tree/ResourceColumn.tsx
    ui/packages/@quent/components/src/resource-tree/ResourceGroupRow.tsx
    ui/packages/@quent/components/src/resource-tree/ResourceRow.tsx
    ui/packages/@quent/components/src/resource-tree/InlineSelector.tsx
    ui/packages/@quent/components/src/resource-tree/UsageColumn.tsx
    ui/packages/@quent/components/src/resource-tree/types.ts
    ui/packages/@quent/components/src/operator-timeline/index.ts
    ui/packages/@quent/components/src/operator-timeline/OperatorGanttChart.tsx
    ui/packages/@quent/components/src/operator-timeline/types.ts
    ui/packages/@quent/components/src/operator-timeline/utils.ts
  </files>
  <read_first>
    ui/src/components/dag/DAGChart.tsx
    ui/src/components/dag/DAGControls.tsx
    ui/src/components/dag/DAGLegend.tsx
    ui/src/components/dag/DAGSettingsPopover.tsx
    ui/src/components/timeline/Timeline.tsx
    ui/src/components/timeline/TimelineController.tsx
    ui/src/components/timeline/TimelineSkeleton.tsx
    ui/src/components/timeline/TimelineToolbar.tsx
    ui/src/components/timeline/TimelineTooltip.tsx
    ui/src/components/timeline/ResourceTimeline.tsx
    ui/src/components/timeline/useTimelineChartColors.ts
    ui/src/components/timeline/types.ts
    ui/src/components/query-plan/QueryPlanNode.tsx
    ui/src/components/query-plan/OperatorStatisticsPopup.tsx
    ui/src/components/resource-tree/ResourceColumn.tsx
    ui/src/components/resource-tree/ResourceGroupRow.tsx
    ui/src/components/resource-tree/ResourceRow.tsx
    ui/src/components/resource-tree/InlineSelector.tsx
    ui/src/components/resource-tree/UsageColumn.tsx
    ui/src/components/resource-tree/types.ts
    ui/src/components/operator-timeline/index.ts
    ui/src/components/operator-timeline/OperatorGanttChart.tsx
    ui/src/components/operator-timeline/types.ts
    ui/src/components/operator-timeline/utils.ts
    ui/packages/@quent/hooks/src/index.ts
  </read_first>
  <action>
    Copy each component directory into the package. For EVERY file, rewrite ALL imports following these rules:

    **Import rewrite rules (apply to EVERY copied file):**
    1. `from '@/components/ui/X'` → `from '../ui/X'` (relative within package)
    2. `from '@/lib/X'` → `from '../lib/X'` (relative to internal lib)
    3. `from '@/services/query-plan/X'` → `from '../services/query-plan/X'` (relative to internal services)
    4. `from '@/atoms/dagControls'` → use `@quent/hooks` selector hooks instead (see below)
    5. `from '@/atoms/dag'` → use `@quent/hooks` named hooks instead (see below)
    6. `from '@/contexts/ThemeContext'` → REMOVE; accept `isDark: boolean` prop instead (see below)
    7. `from '@/types'` → `from '@quent/utils'`
    8. `from '@/hooks/useDeferredReady'` → `from '@quent/hooks'`
    9. `from '~quent/types/X'` → `from '@quent/utils'`
    10. `from '@/services/colors'` → `from '@quent/utils'` (withOpacity is already exported)
    11. `from '@/components/timeline/X'` → `from '../timeline/X'` (relative)

    **ThemeContext decoupling (per D-03, D-04) — affects 5 files:**

    `DAGChart.tsx`: Remove `import { useTheme, THEME_DARK } from '@/contexts/ThemeContext'`. Add `isDark?: boolean` to `DAGChartProps`. Replace `const { theme } = useTheme(); const isDarkMode = theme === THEME_DARK;` with using the `isDark` prop directly (default `false`).

    `DAGLegend.tsx`: Same pattern — remove useTheme import, add `isDark?: boolean` prop, use directly.

    `DAGSettingsPopover.tsx`: Same pattern.

    `useTimelineChartColors.ts`: Change function signature from `useTimelineChartColors()` to `useTimelineChartColors(isDark: boolean)`. Remove `const { theme } = useTheme(); const isDark = theme === THEME_DARK;`. Use the `isDark` parameter directly.

    Components that call `useTimelineChartColors()` (Timeline.tsx, TimelineController.tsx, OperatorGanttChart.tsx) must pass `isDark` to it. These components should also accept `isDark?: boolean` prop and pass it through.

    **DAGChart controlled-first API (per D-08, COMP-02):**

    Add to `DAGChartProps`:
    ```typescript
    export interface DAGChartProps {
      data: DAGData;
      height?: string;
      isDark?: boolean;
      /** Controlled selection — when provided, overrides internal atom state */
      selectedNodeIds?: string[];
      /** Selection change callback — only fires in controlled mode */
      onSelectionChange?: (nodeIds: string[]) => void;
      className?: string;
    }
    ```

    Inside DAGChart:
    ```typescript
    const atomNodeIds = useSelectedNodeIds();
    const setAtomNodeIds = useSetSelectedNodeIds();
    const isControlled = controlledNodeIds !== undefined;
    const activeNodeIds = isControlled ? new Set(controlledNodeIds) : atomNodeIds;
    const handleSelection = isControlled
      ? (ids: Set<string>) => onSelectionChange?.(Array.from(ids))
      : (ids: Set<string>) => setAtomNodeIds(ids);
    ```

    **Atom import replacements (HOOKS-02 compliant):**

    Wherever a component does `useAtomValue(someAtom)` from `@/atoms/dagControls`:
    - `useAtomValue(selectedColorField)` → `const [colorField] = useSelectedColorField()` from `@quent/hooks`
    - `useAtomValue(nodeColoringAtom)` → `useNodeColoringValue()` from `@quent/hooks`
    - `useAtomValue(nodeColorPaletteAtom)` → `const [palette] = useNodeColorPalette()` from `@quent/hooks`
    - `useSetAtom(selectedColorField)` → `const [, setColorField] = useSelectedColorField()` from `@quent/hooks`
    - Same pattern for all dagControls atoms — use the selector hooks from Plan 01.

    For `OperatorGanttChart.tsx` specifically (Pitfall 2):
    - `import { selectedNodeIdsAtom, selectedOperatorLabelAtom, selectedPlanIdAtom } from '@/atoms/dag'` → Replace with:
      - `import { useSelectedNodeIds, useSelectedOperatorLabel, useSelectedPlanId } from '@quent/hooks';`
      - Replace `useAtomValue(selectedNodeIdsAtom)` with `useSelectedNodeIds()`
      - Replace `useAtomValue(selectedOperatorLabelAtom)` with `useSelectedOperatorLabel()` (first element of tuple if it returns [value, setter])
      - Replace `useAtomValue(selectedPlanIdAtom)` with `useSelectedPlanId()` (first element)
    - `import { withOpacity } from '@/services/colors'` → `import { withOpacity } from '@quent/utils'`

    **operator-timeline/utils.ts** (Pitfall, ~quent/types migration):
    - `from '~quent/types/QueryBundle'` → `from '@quent/utils'`
    - `from '~quent/types/EntityRef'` → `from '@quent/utils'`
    - `from '~quent/types/Operator'` → `from '@quent/utils'`
    - `from '~quent/types/PlanTree'` → `from '@quent/utils'`
    - Combine into single import: `import type { QueryBundle, EntityRef, Operator, PlanTree } from '@quent/utils';`

    **JSDoc and props (COMP-04, COMP-05, COMP-06):**
    For every exported component:
    - Ensure it has a JSDoc `/** ... */` comment above the export
    - Ensure its props interface is exported: `export interface DAGChartProps { ... }`
    - Ensure `className?: string` is in the props and applied via `cn()` at the root element

    **className audit (COMP-06)**: Check each component for a root-level `className` prop. Components that are wrappers around third-party libs (ECharts, ReactFlow) may not have a simple root div — apply className to the outermost wrapper div. For hooks like `useTimelineChartColors`, className does not apply.
  </action>
  <verify>
    <automated>cd /Users/johallaron/Projects/quent/ui && npx tsc --noEmit 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - ui/packages/@quent/components/src/dag/DAGChart.tsx exists and contains `selectedNodeIds?: string[]`
    - ui/packages/@quent/components/src/dag/DAGChart.tsx contains `onSelectionChange?: (nodeIds: string[]) => void`
    - ui/packages/@quent/components/src/dag/DAGChart.tsx contains `isDark?: boolean`
    - ui/packages/@quent/components/src/dag/DAGChart.tsx does NOT contain `useTheme` or `ThemeContext`
    - ui/packages/@quent/components/src/dag/DAGLegend.tsx contains `isDark?: boolean`
    - ui/packages/@quent/components/src/dag/DAGLegend.tsx does NOT contain `useTheme` or `ThemeContext`
    - ui/packages/@quent/components/src/dag/DAGSettingsPopover.tsx does NOT contain `useTheme` or `ThemeContext`
    - ui/packages/@quent/components/src/timeline/useTimelineChartColors.ts contains `isDark: boolean` as parameter
    - ui/packages/@quent/components/src/timeline/useTimelineChartColors.ts does NOT contain `useTheme` or `ThemeContext`
    - ui/packages/@quent/components/src/operator-timeline/OperatorGanttChart.tsx does NOT contain `from '@/atoms/dag'`
    - ui/packages/@quent/components/src/operator-timeline/OperatorGanttChart.tsx contains `from '@quent/hooks'`
    - ui/packages/@quent/components/src/operator-timeline/utils.ts does NOT contain `~quent/types`
    - `grep -r "from '@/" ui/packages/@quent/components/src/` returns zero results
    - `grep -r "from '~quent/types" ui/packages/@quent/components/src/` returns zero results
    - `grep -r "ThemeContext" ui/packages/@quent/components/src/` returns zero results
    - All DAG components exist in ui/packages/@quent/components/src/dag/
    - All timeline components exist in ui/packages/@quent/components/src/timeline/
    - All resource-tree components exist in ui/packages/@quent/components/src/resource-tree/
    - All query-plan components exist in ui/packages/@quent/components/src/query-plan/
    - All operator-timeline files exist in ui/packages/@quent/components/src/operator-timeline/
    - `tsc --noEmit` passes from ui/ root
  </acceptance_criteria>
  <done>All complex components are in @quent/components with ThemeContext replaced by isDark prop, DAGChart has controlled-first API, all imports use relative paths or @quent/* packages, no @/ or ~quent/types imports remain.</done>
</task>

<task type="auto">
  <name>Task 3: Create @quent/components barrel export (index.ts) with all named exports</name>
  <files>
    ui/packages/@quent/components/src/index.ts
  </files>
  <read_first>
    ui/packages/@quent/components/src/index.ts
    ui/packages/@quent/utils/src/index.ts
    ui/packages/@quent/hooks/src/index.ts
  </read_first>
  <action>
    Replace the scaffold `export {};` in `ui/packages/@quent/components/src/index.ts` with a complete barrel export (per COMP-07: named exports only, no `export *`).

    The barrel must export every component and its props interface. Internal utilities (lib/, services/) are NOT exported unless app-layer code needs them (per D-06).

    **Exports to determine:** Check which functions/types from `services/query-plan/` are needed by app-layer hooks (`useQueryPlanVisualization.ts`, `useDagControls.ts`):
    - `useQueryPlanVisualization` imports `getTreeData`, `getPlanDAG` from `query-bundle-transformer` and types `QueryPlanDataItem`, `DAGNode`, `DAGEdge` from `types` → These MUST be exported.
    - `useDagControls` (now in @quent/hooks) uses dependency injection → `computeNodeColoring`, `computeEdgeWidthConfig`, `computeEdgeColoring` from `dagFieldProcessing` and `parseCustomStatistics` from `queryBundle.utils` MUST be exported so the app can pass them.
    - `inferFieldFormatter` from `dagFieldProcessing` is used by DAGChart and DAGLegend (internal use only — NOT exported since these components are now in the package).
    - `operationTypes.ts` exports used by DAGChart — internal use only, NOT exported.

    Write the full `index.ts`:

    ```typescript
    // ============================================================
    // @quent/components — public API surface
    // Named exports only (COMP-07). Internal utilities not exported.
    // ============================================================

    // --- UI Primitives ---
    export { Button } from './ui/button';
    export type { ButtonProps } from './ui/button';
    export { Card, CardContent, CardHeader, CardTitle, CardDescription, CardFooter } from './ui/card';
    // (export all sub-components and types from each primitive)
    export { Collapsible, CollapsibleContent, CollapsibleTrigger } from './ui/collapsible';
    export { DataText } from './ui/data-text';
    // export type { DataTextProps } from './ui/data-text';
    export {
      DropdownMenu, DropdownMenuContent, DropdownMenuItem,
      DropdownMenuTrigger, DropdownMenuSeparator, DropdownMenuLabel,
      DropdownMenuGroup, DropdownMenuSub, DropdownMenuSubTrigger,
      DropdownMenuSubContent, DropdownMenuCheckboxItem,
      DropdownMenuRadioGroup, DropdownMenuRadioItem,
    } from './ui/dropdown-menu';
    export { HoverCard, HoverCardContent, HoverCardTrigger } from './ui/hover-card';
    export { Input } from './ui/input';
    // export type { InputProps } from './ui/input';
    export {
      NavigationMenu, NavigationMenuContent, NavigationMenuItem,
      NavigationMenuLink, NavigationMenuList, NavigationMenuTrigger,
      navigationMenuTriggerStyle,
    } from './ui/navigation-menu';
    export { Popover, PopoverContent, PopoverTrigger } from './ui/popover';
    export { ResizableHandle, ResizablePanel, ResizablePanelGroup } from './ui/resizable';
    export { ScrollArea, ScrollBar } from './ui/scroll-area';
    export { SelectField } from './ui/select-field';
    export type { SelectFieldOption } from './ui/select-field';
    export {
      Select, SelectContent, SelectItem, SelectTrigger, SelectValue,
      SelectGroup, SelectLabel, SelectSeparator,
    } from './ui/select';
    export { Skeleton } from './ui/skeleton';
    export { TreeTable } from './ui/tree-table';
    // export type { TreeTableProps } from './ui/tree-table';
    export { TreeView } from './ui/tree-view';
    export type { TreeDataItem } from './ui/tree-view';

    // --- DAG Components ---
    export { DAGChart } from './dag/DAGChart';
    export type { DAGChartProps } from './dag/DAGChart';
    export { DAGControls } from './dag/DAGControls';
    export type { DAGControlsProps } from './dag/DAGControls';
    export { DAGLegend } from './dag/DAGLegend';
    export type { DAGLegendProps } from './dag/DAGLegend';
    export { DAGSettingsPopover } from './dag/DAGSettingsPopover';
    export type { DAGSettingsPopoverProps } from './dag/DAGSettingsPopover';

    // --- Timeline Components ---
    export { Timeline, CHART_GROUP } from './timeline/Timeline';
    export type { TimelineProps } from './timeline/Timeline';
    export { TimelineController } from './timeline/TimelineController';
    export type { TimelineControllerProps } from './timeline/TimelineController';
    export { TimelineSkeleton } from './timeline/TimelineSkeleton';
    export { TimelineToolbar } from './timeline/TimelineToolbar';
    export type { TimelineToolbarProps } from './timeline/TimelineToolbar';
    export { TimelineTooltip } from './timeline/TimelineTooltip';
    export type { TimelineTooltipProps } from './timeline/TimelineTooltip';
    export { ResourceTimeline } from './timeline/ResourceTimeline';
    export type { ResourceTimelineProps } from './timeline/ResourceTimeline';
    export { useTimelineChartColors } from './timeline/useTimelineChartColors';
    export { TIMELINE_SPACING, TIMELINE_X_AXIS_ANIMATION } from './timeline/types';

    // --- Query Plan Components ---
    export { QueryPlanNode } from './query-plan/QueryPlanNode';
    export type { QueryPlanNodeProps } from './query-plan/QueryPlanNode';
    export { OperatorStatisticsPopup } from './query-plan/OperatorStatisticsPopup';
    export type { OperatorStatisticsPopupProps } from './query-plan/OperatorStatisticsPopup';

    // --- Resource Tree Components ---
    export { ResourceColumn } from './resource-tree/ResourceColumn';
    export { ResourceGroupRow } from './resource-tree/ResourceGroupRow';
    export { ResourceRow } from './resource-tree/ResourceRow';
    export { InlineSelector } from './resource-tree/InlineSelector';
    export { UsageColumn } from './resource-tree/UsageColumn';

    // --- Operator Timeline ---
    export { OperatorGanttChart } from './operator-timeline/OperatorGanttChart';
    export type { OperatorGanttChartProps } from './operator-timeline/OperatorGanttChart';

    // --- Types needed by app-layer code ---
    export type { QueryPlanDataItem, DAGData, QueryPlanNodeData } from './services/query-plan/types';

    // --- Functions needed by app-layer hooks ---
    export { getTreeData, getPlanDAG } from './services/query-plan/query-bundle-transformer';
    export { computeNodeColoring, computeEdgeWidthConfig, computeEdgeColoring } from './services/query-plan/dagFieldProcessing';
    export { parseCustomStatistics } from './lib/queryBundle.utils';
    ```

    **IMPORTANT**: The exact exports depend on what each source file actually exports. Read each file's actual export names before writing the barrel. The above is a template — verify each export name exists. For types that are commented with `//` above, check if the type is actually exported from the source file. If it is, include it. If not, add the export to the source file.

    After writing index.ts, verify it compiles by running `tsc --noEmit`.
  </action>
  <verify>
    <automated>cd /Users/johallaron/Projects/quent/ui && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ui/packages/@quent/components/src/index.ts does NOT contain `export {}`
    - ui/packages/@quent/components/src/index.ts does NOT contain `export *`
    - ui/packages/@quent/components/src/index.ts contains `export { Button }` (primitives)
    - ui/packages/@quent/components/src/index.ts contains `export { DAGChart }` (DAG)
    - ui/packages/@quent/components/src/index.ts contains `export type { DAGChartProps }` (props)
    - ui/packages/@quent/components/src/index.ts contains `export { TimelineController }` (timeline)
    - ui/packages/@quent/components/src/index.ts contains `export { OperatorGanttChart }` (operator-timeline)
    - ui/packages/@quent/components/src/index.ts contains `export { QueryPlanNode }` (query-plan)
    - ui/packages/@quent/components/src/index.ts contains `export { ResourceColumn }` (resource-tree)
    - ui/packages/@quent/components/src/index.ts contains `export { getTreeData, getPlanDAG }` (app-layer needs)
    - ui/packages/@quent/components/src/index.ts contains `export { computeNodeColoring` (app-layer needs)
    - ui/packages/@quent/components/src/index.ts contains `export { parseCustomStatistics }` (app-layer needs)
    - ui/packages/@quent/components/src/index.ts does NOT contain `from '../lib/echarts'` (internal, not exported)
    - `tsc --noEmit` passes from ui/ root
  </acceptance_criteria>
  <done>@quent/components has a complete, typed barrel export with all public components, their props interfaces, and the minimal set of functions/types that app-layer code needs. No export * used. Internal utilities remain private.</done>
</task>

</tasks>

<verification>
- `cd /Users/johallaron/Projects/quent/ui && npx tsc --noEmit` passes with zero errors
- `grep -rn "from '@/" ui/packages/@quent/components/src/` returns zero results
- `grep -rn "from '~quent/types" ui/packages/@quent/components/src/` returns zero results
- `grep -rn "ThemeContext" ui/packages/@quent/components/src/` returns zero results
- `grep -c "export {" ui/packages/@quent/components/src/index.ts` returns 30+ (many named exports)
- `grep "export \*" ui/packages/@quent/components/src/index.ts` returns zero results
</verification>

<success_criteria>
All UI primitives, DAG, timeline, resource-tree, query-plan, and operator-timeline components live in @quent/components. ThemeContext is decoupled (isDark prop). DAGChart has controlled-first API. Barrel export is complete. The app still compiles because original source files are still in place.
</success_criteria>

<output>
After completion, create `.planning/phases/04-extract-quent-components-and-migrate-app-shell/04-02-SUMMARY.md`
</output>
