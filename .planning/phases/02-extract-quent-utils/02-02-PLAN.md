---
phase: 02-extract-quent-utils
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - ui/vite.config.ts
  - ui/tsconfig.json
  - ui/src/services/api.ts
  - ui/src/services/api.test.ts
  - ui/src/lib/utils.ts
  - ui/src/services/colors.ts
  - ui/src/services/formatters.ts
  - ui/src/types.ts
  - ui/src/lib/timeline.utils.ts
  - ui/src/lib/queryBundle.utils.ts
  - ui/src/services/query-plan/query-bundle-transformer.ts
  - ui/src/atoms/timeline.ts
  - ui/src/hooks/useQueryBundle.ts
  - ui/src/hooks/useBulkTimelines.ts
  - ui/src/hooks/useBulkTimelineFetch.ts
  - ui/src/hooks/useQueryPlanVisualization.ts
  - ui/src/routes/__root.tsx
  - ui/src/routes/profile.engine.$engineId.query.$queryId.index.tsx
  - ui/src/routes/profile.engine.$engineId.query.$queryId.node.$nodeId.tsx
  - ui/src/pages/EngineSelectionPage.tsx
  - ui/src/components/NavBarNavigator.tsx
  - ui/src/components/QueryResourceTree.tsx
  - ui/src/components/QueryResourceTree.test.tsx
  - ui/src/components/query-plan/QueryPlanNode.tsx
  - ui/src/components/timeline/Timeline.tsx
  - ui/src/components/timeline/TimelineController.tsx
  - ui/src/components/timeline/TimelineTooltip.tsx
  - ui/src/components/timeline/ResourceTimeline.tsx
  - ui/src/components/timeline/useTimelineChartColors.ts
  - ui/src/components/resource-tree/ResourceColumn.tsx
  - ui/src/components/resource-tree/ResourceRow.tsx
  - ui/src/components/resource-tree/ResourceGroupRow.tsx
  - ui/src/components/resource-tree/UsageColumn.tsx
  - ui/src/components/resource-tree/InlineSelector.tsx
  - ui/src/components/ui/button.tsx
  - ui/src/components/ui/card.tsx
  - ui/src/components/ui/dropdown-menu.tsx
  - ui/src/components/ui/hover-card.tsx
  - ui/src/components/ui/input.tsx
  - ui/src/components/ui/navigation-menu.tsx
  - ui/src/components/ui/popover.tsx
  - ui/src/components/ui/resizable.tsx
  - ui/src/components/ui/select.tsx
  - ui/src/components/ui/skeleton.tsx
  - ui/src/components/ui/tree-table.tsx
  - ui/src/components/ui/tree-view.tsx
autonomous: true
requirements: [UTILS-01, UTILS-02, UTILS-03, UTILS-04, UTILS-05]

must_haves:
  truths:
    - "All app files import cn, types, colors, and formatters from @quent/utils — no legacy import paths remain"
    - "The ~quent/types path alias is fully removed from vite.config.ts and tsconfig.json"
    - "api.ts imports parseJsonWithBigInt from @quent/utils and the existing test still passes"
    - "pnpm build succeeds with zero errors"
    - "The original source files (utils.ts, colors.ts, formatters.ts) are deleted — no dead code"
  artifacts:
    - path: "ui/vite.config.ts"
      provides: "Vite config without ~quent/types alias"
    - path: "ui/tsconfig.json"
      provides: "TS config without ~quent/types path mapping or include"
  key_links:
    - from: "ui/src/**/*.{ts,tsx} (40 files)"
      to: "@quent/utils"
      via: "import { ... } from '@quent/utils'"
      pattern: "from '@quent/utils'"
    - from: "ui/src/services/api.ts"
      to: "@quent/utils"
      via: "import { parseJsonWithBigInt } from '@quent/utils'"
      pattern: "parseJsonWithBigInt.*from.*@quent/utils"
---

<objective>
Migrate all app imports to use @quent/utils, remove the ~quent/types path alias, update api.ts to import parseJsonWithBigInt from the package, delete the original source files, and verify the full app builds.

Purpose: Complete the extraction by wiring the app to the package. After this plan, zero legacy import paths remain and the app exclusively uses @quent/utils for cn, types, colors, and formatters.

Output: ~40 files with updated imports, vite.config.ts and tsconfig.json cleaned of aliases, 3 source files deleted, all tests passing, pnpm build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-extract-quent-utils/02-CONTEXT.md
@.planning/phases/02-extract-quent-utils/02-RESEARCH.md
@.planning/phases/02-extract-quent-utils/02-01-SUMMARY.md

@ui/vite.config.ts
@ui/tsconfig.json
@ui/src/services/api.ts
@ui/src/services/api.test.ts

<interfaces>
<!-- The @quent/utils barrel exports (created by Plan 01) -->

From ui/packages/@quent/utils/src/index.ts:
```typescript
// Utilities
export { cn } from './cn';
export { parseJsonWithBigInt } from './parseJsonWithBigInt';

// Color utilities
export { PALETTES, getColorForKey, assignColors, getColorByIndex, withOpacity,
  resetColorAssignments, lightenColor, darkenColor, getActivePalette,
  setActivePalette, getPalette, createStripePattern, createDotPattern,
  createCrosshatchPattern, BLACK, WHITE } from './colors';
export type { PaletteName, ChartColor } from './colors';

// Formatter utilities
export { formatDuration, formatDurationForWindow, formatQuantity } from './formatters';

// All 52 Rust-generated types re-exported via export * from './types/index'
```

Import migration patterns:
- `import { cn } from '@/lib/utils'` --> `import { cn } from '@quent/utils'`
- `import type { Foo } from '~quent/types/Foo'` --> `import type { Foo } from '@quent/utils'`
- `import { Foo } from '~quent/types/Foo'` --> `import { Foo } from '@quent/utils'` (value imports of type-only exports)
- `import { withOpacity } from '@/services/colors'` --> `import { withOpacity } from '@quent/utils'`
- `import { formatDuration } from '@/services/formatters'` --> `import { formatDuration } from '@quent/utils'`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all app imports to @quent/utils</name>
  <read_first>
    ui/vite.config.ts
    ui/tsconfig.json
    ui/src/services/api.ts
    ui/src/services/api.test.ts
    ui/src/types.ts
    ui/src/lib/timeline.utils.ts
    ui/src/lib/queryBundle.utils.ts
    ui/src/services/query-plan/query-bundle-transformer.ts
  </read_first>
  <files>
    All 40+ files listed in files_modified above (every file in ui/src/ that imports from ~quent/types/*, @/lib/utils, @/services/colors, or @/services/formatters)
  </files>
  <action>
Perform four import migration sweeps across ui/src/. For each file, consolidate multiple ~quent/types imports into a single `import type { ... } from '@quent/utils'` line (or `import { ... } from '@quent/utils'` for value imports). When a file already has an `@quent/utils` import from a previous sweep, merge new symbols into the existing import statement.

**Sweep 1: ~quent/types/* --> @quent/utils** (22 files, ~80 import lines)

For each file with `~quent/types` imports, replace ALL individual type imports with a single consolidated import from `@quent/utils`. Example transformation for ui/src/services/api.ts:

FROM (lines 9-19):
```typescript
import type { QueryBundle } from '~quent/types/QueryBundle';
import type { QueryGroup } from '~quent/types/QueryGroup';
import type { Query } from '~quent/types/Query';
import type { BulkTimelinesResponse } from '~quent/types/BulkTimelinesResponse';
import type { SingleTimelineRequest } from '~quent/types/SingleTimelineRequest';
import type { SingleTimelineResponse } from '~quent/types/SingleTimelineResponse';
import type { BulkTimelineRequest } from '~quent/types/BulkTimelineRequest';
import type { QueryFilter } from '~quent/types/QueryFilter';
import type { TaskFilter } from '~quent/types/TaskFilter';
import type { EntityRef } from '~quent/types/EntityRef';
import type { Engine } from '~quent/types/Engine';
```

TO:
```typescript
import type { QueryBundle, QueryGroup, Query, BulkTimelinesResponse, SingleTimelineRequest, SingleTimelineResponse, BulkTimelineRequest, QueryFilter, TaskFilter, EntityRef, Engine } from '@quent/utils';
```

IMPORTANT: Some files use value imports (no `type` keyword), e.g. `import { Operator } from '~quent/types/Operator'`. These are used as runtime values (even though they are type aliases). Keep them as value imports: `import { Operator } from '@quent/utils'`. Do NOT add `type` keyword to imports that don't already have it. Files with mixed type/value imports from ~quent/types should have TWO import lines: one `import type { ... }` and one `import { ... }`.

Files with ~quent/types imports (22 files — read each before modifying):
- ui/src/services/api.ts (11 type imports — also handle parseJsonWithBigInt in this sweep, see below)
- ui/src/services/formatters.ts (SKIP — will be deleted in Task 2)
- ui/src/services/query-plan/query-bundle-transformer.ts (2 type + 4 value imports)
- ui/src/types.ts (11 value imports)
- ui/src/lib/timeline.utils.ts (10 type+value imports)
- ui/src/lib/queryBundle.utils.ts (2 value imports)
- ui/src/atoms/timeline.ts (3 type imports)
- ui/src/hooks/useQueryBundle.ts (2 type imports)
- ui/src/hooks/useBulkTimelines.ts (3 type imports)
- ui/src/hooks/useBulkTimelineFetch.ts (3 type imports)
- ui/src/hooks/useQueryPlanVisualization.ts (2 type imports)
- ui/src/routes/profile.engine.$engineId.query.$queryId.index.tsx (1 value + 1 type import)
- ui/src/routes/profile.engine.$engineId.query.$queryId.node.$nodeId.tsx (1 value + 1 type import)
- ui/src/components/QueryResourceTree.tsx (3 value + 3 type imports)
- ui/src/components/QueryResourceTree.test.tsx (3 type imports)
- ui/src/components/query-plan/QueryPlanNode.tsx (1 value import)
- ui/src/components/timeline/TimelineController.tsx (1 type import)
- ui/src/components/timeline/ResourceTimeline.tsx (5 type imports)
- ui/src/components/resource-tree/ResourceColumn.tsx (2 value imports)
- ui/src/components/resource-tree/ResourceRow.tsx (1 value import)
- ui/src/components/resource-tree/ResourceGroupRow.tsx (1 value import)
- ui/src/components/resource-tree/UsageColumn.tsx (1 value + 1 type import)

**Also in api.ts:** Per D-07, D-08: Remove the `parseJsonWithBigInt` function definition (lines 26-53) from api.ts and add `import { parseJsonWithBigInt } from '@quent/utils';` at the top. The `parseJsonWithBigInt` function is still used within api.ts for response parsing — it just comes from the package now. Keep `ChartDataPoint`, `BarChartData`, `DEFAULT_STALE_TIME`, and all other api.ts content intact.

**Sweep 2: @/lib/utils --> @quent/utils** (18 files)

Replace `import { cn } from '@/lib/utils'` with `import { cn } from '@quent/utils'`. If the file already has an `@quent/utils` import from Sweep 1, merge `cn` into that import.

Files (18):
- ui/src/routes/__root.tsx
- ui/src/pages/EngineSelectionPage.tsx
- ui/src/components/NavBarNavigator.tsx
- ui/src/components/timeline/TimelineTooltip.tsx
- ui/src/components/ui/button.tsx
- ui/src/components/ui/card.tsx
- ui/src/components/ui/dropdown-menu.tsx
- ui/src/components/ui/hover-card.tsx
- ui/src/components/ui/input.tsx
- ui/src/components/ui/navigation-menu.tsx
- ui/src/components/ui/popover.tsx
- ui/src/components/ui/resizable.tsx
- ui/src/components/ui/select.tsx
- ui/src/components/ui/skeleton.tsx
- ui/src/components/ui/tree-table.tsx
- ui/src/components/ui/tree-view.tsx
- ui/src/components/resource-tree/ResourceColumn.tsx
- ui/src/components/resource-tree/InlineSelector.tsx

**Sweep 3: @/services/colors --> @quent/utils** (5 files)

Replace `import { ... } from '@/services/colors'` with `import { ... } from '@quent/utils'`. Merge into existing `@quent/utils` import if present.

Files (5):
- ui/src/lib/timeline.utils.ts (getColorForKey, WHITE, withOpacity)
- ui/src/components/timeline/useTimelineChartColors.ts (BLACK, WHITE, withOpacity)
- ui/src/components/timeline/TimelineController.tsx (withOpacity)
- ui/src/components/timeline/Timeline.tsx (withOpacity)
- ui/src/components/timeline/ResourceTimeline.tsx (WHITE, withOpacity)

**Sweep 4: @/services/formatters --> @quent/utils** (3 files)

Replace `import { ... } from '@/services/formatters'` with `import { ... } from '@quent/utils'`. Merge into existing `@quent/utils` import if present.

Files (3):
- ui/src/lib/timeline.utils.ts (formatQuantity)
- ui/src/components/timeline/TimelineTooltip.tsx (formatDurationForWindow)
- ui/src/components/timeline/TimelineController.tsx (formatDuration)

**Sweep 5: api.test.ts import update** (1 file)

Per Research Pitfall 5: Update `ui/src/services/api.test.ts` line 5:
FROM: `import { parseJsonWithBigInt } from './api';`
TO: `import { parseJsonWithBigInt } from '@quent/utils';`

After all sweeps, verify no legacy imports remain:
```bash
grep -r "~quent/types\|@/lib/utils\|@/services/colors\|@/services/formatters" ui/src/ --include='*.ts' --include='*.tsx' | grep -v node_modules
```
This must return zero results (formatters.ts will be deleted in Task 2, so if it shows up, that is OK temporarily).
  </action>
  <verify>
    <automated>cd /Users/johallaron/Projects/quent && grep -rn "~quent/types" ui/src/ --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v "formatters.ts" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - Zero files in ui/src/ contain `~quent/types` (except formatters.ts which is deleted in Task 2)
    - Zero files in ui/src/ contain `@/lib/utils`
    - Zero files in ui/src/ contain `@/services/colors`
    - Zero files in ui/src/ contain `@/services/formatters`
    - ui/src/services/api.ts contains `import { parseJsonWithBigInt } from '@quent/utils'`
    - ui/src/services/api.ts does NOT contain `export function parseJsonWithBigInt`
    - ui/src/services/api.test.ts contains `from '@quent/utils'`
    - ui/src/types.ts contains `from '@quent/utils'` (not `~quent/types`)
    - ui/src/components/ui/button.tsx contains `from '@quent/utils'` (not `@/lib/utils`)
  </acceptance_criteria>
  <done>All 40+ app files import exclusively from @quent/utils; no legacy import paths remain in ui/src/ (except files to be deleted)</done>
</task>

<task type="auto">
  <name>Task 2: Remove aliases, delete old source files, verify build</name>
  <read_first>
    ui/vite.config.ts
    ui/tsconfig.json
    ui/src/lib/utils.ts
    ui/src/services/colors.ts
    ui/src/services/formatters.ts
  </read_first>
  <files>
    ui/vite.config.ts
    ui/tsconfig.json
    ui/src/lib/utils.ts (DELETE)
    ui/src/services/colors.ts (DELETE)
    ui/src/services/formatters.ts (DELETE)
  </files>
  <action>
1. **Remove ~quent/types alias from vite.config.ts** — In ui/vite.config.ts, remove line 68:
   ```
   '~quent/types': path.resolve(__dirname, '../examples/simulator/server/ts-bindings'),
   ```
   And remove the TODO comment on lines 66-67. The resolve.alias section should become:
   ```typescript
   alias: {
     '@': path.resolve(__dirname, './src'),
     // Force elkjs to use bundled version (avoids web-worker module resolution issues)
     elkjs: 'elkjs/lib/elk.bundled.js',
   },
   ```
   Per D-02: the alias is replaced by @quent/utils imports.

2. **Remove ~quent/types from tsconfig.json** — In ui/tsconfig.json:
   - Remove line 28: `"~quent/types/*": ["../examples/simulator/server/ts-bindings/*"],` from compilerOptions.paths
   - Remove `"../examples/simulator/server/ts-bindings"` from the include array (line 31). The include should become: `"include": ["src"],`
   Per Research Pitfall 6: remove BOTH the paths entry AND the include entry. The @quent/utils package's own tsconfig handles type visibility via its barrel imports.

3. **Delete old source files** — Per D-12 (Claude's discretion — prefer deletion if no other app code in those files):
   - Delete `ui/src/lib/utils.ts` — contains only cn() (9 lines, confirmed). Use `rm ui/src/lib/utils.ts`.
   - Delete `ui/src/services/colors.ts` — contains only color utilities (291 lines, confirmed). Use `rm ui/src/services/colors.ts`.
   - Delete `ui/src/services/formatters.ts` — contains only formatter utilities (143 lines, confirmed). Use `rm ui/src/services/formatters.ts`.

   After deletion, verify no remaining imports reference these files:
   ```bash
   grep -rn "from.*\./colors\|from.*\./formatters\|from.*\./utils" ui/src/lib/ ui/src/services/ --include='*.ts' --include='*.tsx' 2>/dev/null
   ```

4. **Run full build and tests:**
   ```bash
   cd /Users/johallaron/Projects/quent/ui && pnpm tsc --noEmit && pnpm test:run && pnpm build
   ```
   - `tsc --noEmit` validates all import paths resolve from ui/tsconfig.json (the app's perspective)
   - `pnpm test:run` validates existing tests (especially api.test.ts with updated import) pass
   - `pnpm build` validates production bundle succeeds (Vite resolves @quent/utils correctly)

   If tsc fails with errors about types not found: the @quent/utils tsconfig (with composite: true and project references in ui/tsconfig.json) should handle cross-package type visibility. If not, check that the `"references"` array in ui/tsconfig.json includes `{ "path": "./packages/@quent/utils" }` (it already does from Phase 1).
  </action>
  <verify>
    <automated>cd /Users/johallaron/Projects/quent/ui && pnpm build && pnpm test:run && test ! -f src/lib/utils.ts && test ! -f src/services/colors.ts && test ! -f src/services/formatters.ts</automated>
  </verify>
  <acceptance_criteria>
    - ui/vite.config.ts does NOT contain `~quent/types`
    - ui/vite.config.ts does NOT contain `TODO: Using ts bindings`
    - ui/tsconfig.json does NOT contain `~quent/types`
    - ui/tsconfig.json include array is `["src"]` (no ts-bindings entry)
    - ui/src/lib/utils.ts does NOT exist (deleted)
    - ui/src/services/colors.ts does NOT exist (deleted)
    - ui/src/services/formatters.ts does NOT exist (deleted)
    - `pnpm tsc --noEmit` from ui/ exits with code 0
    - `pnpm test:run` from ui/ exits with code 0 (api.test.ts passes)
    - `pnpm build` from ui/ exits with code 0
    - `grep -rn "~quent/types\|@/lib/utils\|@/services/colors\|@/services/formatters" ui/src/ --include='*.ts' --include='*.tsx'` returns zero results
  </acceptance_criteria>
  <done>All legacy aliases removed, old source files deleted, full build and test suite pass with zero errors</done>
</task>

</tasks>

<verification>
1. `cd /Users/johallaron/Projects/quent && grep -rn "~quent/types\|@/lib/utils\|@/services/colors\|@/services/formatters" ui/src/ --include='*.ts' --include='*.tsx'` — returns zero results
2. `cd /Users/johallaron/Projects/quent/ui && pnpm build` — exits 0
3. `cd /Users/johallaron/Projects/quent/ui && pnpm test:run` — exits 0 (api.test.ts parseJsonWithBigInt tests pass)
4. `cd /Users/johallaron/Projects/quent/ui && grep "~quent/types" vite.config.ts tsconfig.json` — returns zero results
5. `test ! -f ui/src/lib/utils.ts && test ! -f ui/src/services/colors.ts && test ! -f ui/src/services/formatters.ts` — all deleted
</verification>

<success_criteria>
- Zero legacy import paths remain in ui/src/ — all point to @quent/utils
- ~quent/types alias removed from both vite.config.ts and tsconfig.json
- Original source files (utils.ts, colors.ts, formatters.ts) deleted
- parseJsonWithBigInt imported from @quent/utils in api.ts; function removed from api.ts
- Full build (pnpm build) succeeds with zero errors
- All tests pass (pnpm test:run), especially parseJsonWithBigInt tests in api.test.ts
</success_criteria>

<output>
After completion, create `.planning/phases/02-extract-quent-utils/02-02-SUMMARY.md`
</output>
