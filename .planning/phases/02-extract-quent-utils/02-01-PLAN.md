---
phase: 02-extract-quent-utils
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/packages/@quent/utils/src/index.ts
  - ui/packages/@quent/utils/src/cn.ts
  - ui/packages/@quent/utils/src/parseJsonWithBigInt.ts
  - ui/packages/@quent/utils/src/colors.ts
  - ui/packages/@quent/utils/src/formatters.ts
  - ui/packages/@quent/utils/src/types/index.ts
  - ui/packages/@quent/utils/package.json
autonomous: true
requirements: [UTILS-01, UTILS-02, UTILS-03, UTILS-04, UTILS-05]

must_haves:
  truths:
    - "cn() is importable from @quent/utils and composes class names"
    - "All 52 Rust-generated types are re-exported from @quent/utils"
    - "parseJsonWithBigInt is importable from @quent/utils"
    - "Color utilities (PALETTES, getColorForKey, assignColors, etc.) are importable from @quent/utils"
    - "Formatter utilities (formatDuration, formatDurationForWindow, formatQuantity) are importable from @quent/utils"
  artifacts:
    - path: "ui/packages/@quent/utils/src/cn.ts"
      provides: "cn() function"
      contains: "export function cn"
    - path: "ui/packages/@quent/utils/src/parseJsonWithBigInt.ts"
      provides: "BigInt JSON parser"
      contains: "export function parseJsonWithBigInt"
    - path: "ui/packages/@quent/utils/src/colors.ts"
      provides: "Color palette and utility functions"
      contains: "export const PALETTES"
    - path: "ui/packages/@quent/utils/src/formatters.ts"
      provides: "Duration and quantity formatters"
      contains: "export function formatDuration"
    - path: "ui/packages/@quent/utils/src/types/index.ts"
      provides: "Barrel re-export of 52 Rust-generated types"
      contains: "export type { QueryBundle }"
    - path: "ui/packages/@quent/utils/src/index.ts"
      provides: "Public barrel with named exports"
      contains: "export { cn }"
  key_links:
    - from: "ui/packages/@quent/utils/src/formatters.ts"
      to: "ui/packages/@quent/utils/src/types/index.ts"
      via: "relative import for PrefixSystem, QuantitySpec, CapacityKind"
      pattern: "from './types/index'"
    - from: "ui/packages/@quent/utils/src/types/index.ts"
      to: "examples/simulator/server/ts-bindings/"
      via: "relative path re-exports (6 levels up)"
      pattern: "from '../../../../../../examples/simulator/server/ts-bindings/"
    - from: "ui/packages/@quent/utils/src/index.ts"
      to: "all source modules"
      via: "named re-exports"
      pattern: "export .* from './"
---

<objective>
Populate the @quent/utils package with all five utility modules: cn(), parseJsonWithBigInt, colors, formatters, and the Rust-generated types barrel.

Purpose: Create the fully-functional @quent/utils package that Plan 02 will wire into the app via import migration. This is the zero-dependency foundation package that all other @quent/* packages will depend on.

Output: 6 source files in ui/packages/@quent/utils/src/, updated package.json with clsx + tailwind-merge dependencies. Package typechecks independently.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-extract-quent-utils/02-CONTEXT.md
@.planning/phases/02-extract-quent-utils/02-RESEARCH.md

@ui/packages/@quent/utils/package.json
@ui/packages/@quent/utils/tsconfig.json
@ui/packages/@quent/utils/src/index.ts
@ui/src/lib/utils.ts
@ui/src/services/colors.ts
@ui/src/services/formatters.ts
@ui/src/services/api.ts

<interfaces>
<!-- Source files to extract from — executor reads these to copy verbatim -->

From ui/src/lib/utils.ts (9 lines — copy entire function):
```typescript
import { type ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

From ui/src/services/api.ts lines 31-53 (parseJsonWithBigInt):
```typescript
export function parseJsonWithBigInt<T>(text: string): T { ... }
```

From ui/src/services/colors.ts (291 lines — copy entire file):
- Exports: PALETTES, PaletteName, ChartColor, getActivePalette, setActivePalette, getPalette, getColorForKey, assignColors, getColorByIndex, withOpacity, resetColorAssignments, lightenColor, darkenColor, createStripePattern, createDotPattern, createCrosshatchPattern, BLACK, WHITE

From ui/src/services/formatters.ts (143 lines — copy entire file, update imports):
- Exports: formatDuration, formatDurationForWindow, formatQuantity
- Current imports: `import type { PrefixSystem } from '~quent/types/PrefixSystem'` (3 type imports)
- After move: change to `import type { PrefixSystem, QuantitySpec, CapacityKind } from './types/index'`

52 Rust-generated type files in examples/simulator/server/ts-bindings/:
- Each exports one `export type { TypeName }`
- Relative path from src/types/ to ts-bindings: ../../../../../../examples/simulator/server/ts-bindings/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package source files and types barrel</name>
  <read_first>
    ui/src/lib/utils.ts
    ui/src/services/colors.ts
    ui/src/services/formatters.ts
    ui/src/services/api.ts
    ui/packages/@quent/utils/src/index.ts
    ui/packages/@quent/utils/package.json
    ui/packages/@quent/utils/tsconfig.json
  </read_first>
  <files>
    ui/packages/@quent/utils/src/cn.ts
    ui/packages/@quent/utils/src/parseJsonWithBigInt.ts
    ui/packages/@quent/utils/src/colors.ts
    ui/packages/@quent/utils/src/formatters.ts
    ui/packages/@quent/utils/src/types/index.ts
  </files>
  <action>
Create five source files in ui/packages/@quent/utils/src/:

1. **cn.ts** — Copy verbatim from ui/src/lib/utils.ts (lines 4-9, without the SPDX header):
   ```typescript
   import { type ClassValue, clsx } from 'clsx';
   import { twMerge } from 'tailwind-merge';
   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs));
   }
   ```
   Per D-10: clsx and tailwind-merge are direct dependencies (added in Task 2).

2. **parseJsonWithBigInt.ts** — Copy the `parseJsonWithBigInt` function from ui/src/services/api.ts lines 31-53. Include the JSDoc comment from lines 26-30. Do NOT copy `ChartDataPoint`, `BarChartData`, `DEFAULT_STALE_TIME`, or any other exports from api.ts — only `parseJsonWithBigInt`. Per D-07.

3. **colors.ts** — Copy entire content of ui/src/services/colors.ts verbatim (all 291 lines). No import changes needed — this file has no external imports. Per D-04, D-05, D-06: preserve module-level mutable state, include canvas pattern functions, add `@remarks Browser-only — requires \`document.createElement('canvas')\`. Do not call in SSR or Node.js environments.` JSDoc annotation to `createStripePattern`, `createDotPattern`, and `createCrosshatchPattern`.

4. **formatters.ts** — Copy entire content of ui/src/services/formatters.ts. Change the three `~quent/types/*` imports at lines 8-10:
   FROM:
   ```typescript
   import type { PrefixSystem } from '~quent/types/PrefixSystem';
   import type { QuantitySpec } from '~quent/types/QuantitySpec';
   import type { CapacityKind } from '~quent/types/CapacityKind';
   ```
   TO:
   ```typescript
   import type { PrefixSystem, QuantitySpec, CapacityKind } from './types/index';
   ```
   Per D-09: intra-package relative import, no circular dependency.

5. **types/index.ts** — Create barrel re-exporting all 52 Rust-generated type files. Each line uses `export type { TypeName } from '../../../../../../examples/simulator/server/ts-bindings/TypeName';`. The 52 types are (alphabetical):
   Attribute, BinnedSpanSec, BulkTimelineRequest, BulkTimelinesResponse, BulkTimelinesResponseEntry, CapacityDecl, CapacityKind, Edge, Engine, EngineImplementationAttributes, EntityFilter, EntityRef, FiniteStateMachine, FsmStateTypeDecl, FsmTransition, FsmTransitionDecl, FsmTypeDecl, FsmUsage, List, Operator, OperatorStatistics, Plan, PlanTree, Port, PortStatistics, PrefixSystem, QuantitySpec, Query, QueryBundle, QueryEntities, QueryFilter, QueryGroup, Resource, ResourceGroup, ResourceGroupNode, ResourceGroupTimelineRequest, ResourceGroupTypeDecl, ResourceTimeline, ResourceTimelineBinned, ResourceTimelineBinnedByState, ResourceTimelineRequest, ResourceTree, ResourceTypeDecl, SingleTimelineRequest, SingleTimelineResponse, SpanSec, Struct, TaskFilter, TimelineConfig, TimelineRequest, Value, Worker.

   Relative path: from ui/packages/@quent/utils/src/types/ to examples/simulator/server/ts-bindings/ is 6 levels up: `../../../../../../examples/simulator/server/ts-bindings/TypeName`. Per D-01, D-03: the ts-bindings directory remains the source of truth; we only re-export.

   IMPORTANT: Do NOT modify any files inside examples/simulator/server/ts-bindings/. Per Research Pitfall 3, their internal relative imports must stay intact.
  </action>
  <verify>
    <automated>cd /Users/johallaron/Projects/quent/ui && ls packages/@quent/utils/src/cn.ts packages/@quent/utils/src/parseJsonWithBigInt.ts packages/@quent/utils/src/colors.ts packages/@quent/utils/src/formatters.ts packages/@quent/utils/src/types/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - ui/packages/@quent/utils/src/cn.ts contains `export function cn(`
    - ui/packages/@quent/utils/src/parseJsonWithBigInt.ts contains `export function parseJsonWithBigInt<T>(text: string): T`
    - ui/packages/@quent/utils/src/colors.ts contains `export const PALETTES`
    - ui/packages/@quent/utils/src/colors.ts contains `@remarks Browser-only` (on canvas pattern functions)
    - ui/packages/@quent/utils/src/formatters.ts contains `from './types/index'` (NOT `~quent/types`)
    - ui/packages/@quent/utils/src/formatters.ts contains `export function formatDuration`
    - ui/packages/@quent/utils/src/types/index.ts contains exactly 52 `export type` lines
    - ui/packages/@quent/utils/src/types/index.ts contains `../../../../../../examples/simulator/server/ts-bindings/` in every import path
    - No files inside examples/simulator/server/ts-bindings/ are modified
  </acceptance_criteria>
  <done>All five source files and the types barrel exist with correct content and import paths</done>
</task>

<task type="auto">
  <name>Task 2: Update package.json dependencies and create index barrel</name>
  <read_first>
    ui/packages/@quent/utils/package.json
    ui/packages/@quent/utils/src/cn.ts
    ui/packages/@quent/utils/src/colors.ts
    ui/packages/@quent/utils/src/formatters.ts
    ui/packages/@quent/utils/src/parseJsonWithBigInt.ts
  </read_first>
  <files>
    ui/packages/@quent/utils/package.json
    ui/packages/@quent/utils/src/index.ts
  </files>
  <action>
1. **Add dependencies to package.json** — Run `cd /Users/johallaron/Projects/quent/ui && pnpm --filter @quent/utils add clsx tailwind-merge` to add clsx and tailwind-merge as direct dependencies. Per D-10, D-13, D-14: these are implementation details (direct deps), not peer contracts. No React/Jotai/TanStack deps.

2. **Replace src/index.ts** — Replace the scaffold `export {}` with the full public barrel. Use named exports for utilities and `export * from './types/index'` for the types sub-barrel (per Research Pattern 5 recommendation: 52 individual type re-exports would make the root barrel unreadable).

   The barrel content must be:
   ```typescript
   // Utilities
   export { cn } from './cn';
   export { parseJsonWithBigInt } from './parseJsonWithBigInt';

   // Color utilities
   export {
     PALETTES,
     getColorForKey,
     assignColors,
     getColorByIndex,
     withOpacity,
     resetColorAssignments,
     lightenColor,
     darkenColor,
     getActivePalette,
     setActivePalette,
     getPalette,
     createStripePattern,
     createDotPattern,
     createCrosshatchPattern,
     BLACK,
     WHITE,
   } from './colors';
   export type { PaletteName, ChartColor } from './colors';

   // Formatter utilities
   export {
     formatDuration,
     formatDurationForWindow,
     formatQuantity,
   } from './formatters';

   // Rust-generated TypeScript types
   export * from './types/index';
   ```

3. **Run typecheck** — `cd /Users/johallaron/Projects/quent/ui && pnpm --filter @quent/utils exec tsc --noEmit` must pass. This validates all imports resolve: cn.ts can find clsx/tailwind-merge, formatters.ts can find types via the barrel, types barrel can reach the ts-bindings files.

   If typecheck fails on the types barrel due to the @quent/utils tsconfig only including `src/` (and not the external ts-bindings directory), update `ui/packages/@quent/utils/tsconfig.json` to add `"../../../../../../examples/simulator/server/ts-bindings"` to the `"include"` array. This allows TypeScript to see the type files that the barrel imports from. The alternative — not needed here — would be project references.
  </action>
  <verify>
    <automated>cd /Users/johallaron/Projects/quent/ui && pnpm --filter @quent/utils exec tsc --noEmit && node -e "const pkg = require('./packages/@quent/utils/package.json'); if (!pkg.dependencies?.clsx || !pkg.dependencies?.['tailwind-merge']) throw new Error('missing deps')"</automated>
  </verify>
  <acceptance_criteria>
    - ui/packages/@quent/utils/package.json contains `"clsx"` in `dependencies`
    - ui/packages/@quent/utils/package.json contains `"tailwind-merge"` in `dependencies`
    - ui/packages/@quent/utils/package.json does NOT contain `react` or `jotai` in any dependency field
    - ui/packages/@quent/utils/src/index.ts contains `export { cn } from './cn'`
    - ui/packages/@quent/utils/src/index.ts contains `export { parseJsonWithBigInt } from './parseJsonWithBigInt'`
    - ui/packages/@quent/utils/src/index.ts contains `export * from './types/index'`
    - ui/packages/@quent/utils/src/index.ts contains `export { formatDuration`
    - ui/packages/@quent/utils/src/index.ts contains `PALETTES` in an export statement
    - `pnpm --filter @quent/utils exec tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>@quent/utils package has all dependencies declared and a complete index barrel; typecheck passes independently</done>
</task>

</tasks>

<verification>
1. `cd /Users/johallaron/Projects/quent/ui && pnpm --filter @quent/utils exec tsc --noEmit` — package typechecks in isolation
2. `cd /Users/johallaron/Projects/quent/ui && ls packages/@quent/utils/src/{cn,parseJsonWithBigInt,colors,formatters}.ts packages/@quent/utils/src/types/index.ts` — all source files exist
3. `cd /Users/johallaron/Projects/quent/ui && grep -c "export type" packages/@quent/utils/src/types/index.ts` — returns 52
</verification>

<success_criteria>
- All 5 source modules exist in ui/packages/@quent/utils/src/
- Types barrel re-exports all 52 Rust-generated types via correct relative paths
- Package typechecks independently (tsc --noEmit passes)
- clsx and tailwind-merge declared as direct dependencies in package.json
- No React/Jotai/TanStack dependencies (zero-framework-dep package per D-14)
- Index barrel exports all public APIs by name
</success_criteria>

<output>
After completion, create `.planning/phases/02-extract-quent-utils/02-01-SUMMARY.md`
</output>
