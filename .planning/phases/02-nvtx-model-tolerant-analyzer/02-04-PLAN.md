---
phase: 02-nvtx-model-tolerant-analyzer
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-03"]
files_modified:
  - integrations/nvtx/analyzer/src/ranges.rs
  - integrations/nvtx/analyzer/src/model.rs
  - integrations/nvtx/analyzer/tests/pushpop.rs
autonomous: true
requirements: [ANA-03, ANA-05]

must_haves:
  truths:
    - "Push/Pop reconstruct as per-(thread_id, domain) nested stacks; a Pop matches the most recent Push on the same thread"
    - "Concurrent interleaved threads nest independently (a Pop never closes another thread's Push)"
    - "A Push never popped is closed at trace-end and flagged synthetic"
    - "An orphan Pop (no open Push on that thread+domain) is skipped without panic"
    - "A full malformed stream analyzes to completion with no panic or abort"
  artifacts:
    - path: "integrations/nvtx/analyzer/src/ranges.rs"
      provides: "per-(thread_id, domain) push/pop stacks with parent capture"
      contains: "thread_id"
  key_links:
    - from: "integrations/nvtx/analyzer/src/ranges.rs"
      to: "(thread_id, domain) stack key"
      via: "RangePush push / RangePop pop innermost"
      pattern: "thread_id.*domain"
    - from: "integrations/nvtx/analyzer/src/model.rs"
      to: "ranges.rs push/pop dispatch"
      via: "pass-2 replay wiring"
      pattern: "RangePush"
---

<objective>
Reconstruct Push/Pop ranges as real per-thread nested stacks (ANA-03) on the `thread_id` added in
Plan 01. Each `RangePush` pushes onto a stack keyed by `(thread_id, domain)`; each `RangePop` pops
the innermost open push on the SAME `(thread_id, domain)`, recording `parent` at pop time
(RESEARCH §Nested-range representation). Pushes left open at end-of-trace close synthetically
(D-13); orphan pops are skipped (ANA-05). A combined malformed-stream integration test proves the
whole core analyzes to completion with no panic (success criterion 3).

Do NOT ship a global/best-effort stack (RESEARCH Pitfall 1) — per-thread stacks are mandatory and
now possible because Plan 01 stamps `thread_id` on Push/Pop.

Purpose: satisfy ANA-03 fully and close the tolerance requirement for the Push/Pop path.
Output: correctly-nested, per-thread Push/Pop spans + a full malformed-stream completion proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-nvtx-model-tolerant-analyzer/02-CONTEXT.md
@.planning/phases/02-nvtx-model-tolerant-analyzer/02-RESEARCH.md
@.planning/phases/02-nvtx-model-tolerant-analyzer/02-PATTERNS.md
@.planning/phases/02-nvtx-model-tolerant-analyzer/02-02-SUMMARY.md
@.planning/phases/02-nvtx-model-tolerant-analyzer/02-03-SUMMARY.md

<interfaces>
<!-- Extracted from source; do not re-explore. -->

From integrations/nvtx/events/src/lib.rs (POST Plan 01 — thread_id now present):
  RangePush { domain: u64, thread_id: u32, attributes: NvtxEventAttributes }
  RangePop  { domain: u64, thread_id: u32 }

Grain reference — injection already nests per (thread, domain) (integrations/nvtx/injection/src/init.rs :56-82):
  range_push_level(domain) / range_pop_level(domain) over a thread_local HashMap<domain, level>.
  The analyzer mirrors this grain but keys by the explicit (thread_id, domain) and stores a Vec<OpenSpan> stack.

From integrations/nvtx/analyzer/src/span.rs:
  NvtxSpan { .., thread_id: Option<u32>, kind: SpanKind, parent: Option<SpanId>, synthetic_end: bool, .. }
  SpanKind::PushPop

From Plan 02/03 model.rs: pass-2 replay loop over timestamp-ordered events + tables in scope (resolve_message).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing per-thread Push/Pop + tolerance tests (RED)</name>
  <files>integrations/nvtx/analyzer/tests/pushpop.rs</files>
  <read_first>
    - integrations/nvtx/analyzer/tests/fixtures.rs (the synthetic Event<NvtxEventEntity> helper)
    - integrations/nvtx/analyzer/tests/reconstruction.rs (existing assertion style)
    - integrations/nvtx/events/src/lib.rs (RangePush/RangePop with thread_id)
    - .planning/phases/02-nvtx-model-tolerant-analyzer/02-RESEARCH.md (§Range Reconstruction Mechanics Push/Pop :270-303; Pitfall 1)
    - .planning/phases/02-nvtx-model-tolerant-analyzer/02-VALIDATION.md (test names)
  </read_first>
  <action>
    Extend the fixture helper (if needed) with RangePush/RangePop constructors taking `thread_id`,
    `domain`, and a timestamp. Add `tests/pushpop.rs` with FAILING tests:
    (1) `pushpop_nested_per_thread` — two threads interleaved in timestamp order (T1 push A, T2 push B,
        T1 push C, T1 pop C, T1 pop A, T2 pop B): assert T1 produces A(parent None) and C(parent A) nested,
        T2 produces B independent; a Pop never closes the other thread's Push.
    (2) `pushpop_single_thread` — a single-thread nested push/pop reconstructs correct parent nesting
        (interim analog to the real-capture case, valid regardless of concurrency).
    (3) `unclosed_closed_at_trace_end` — a Push with no matching Pop closes at max observed ts with
        `synthetic_end = true`.
    (4) `orphan_pop_skipped` — a Pop with no open Push on that (thread,domain) is skipped, no panic, no span.
    (5) `malformed_stream_completes` — an integration stream mixing unclosed pushes, orphan pops, unmatched
        RangeStart, orphan RangeEnd, out-of-order, and duplicate timestamps `build`s to completion and
        returns `Ok` with no panic.
    Reference existing model accessors so tests fail on assertions.
  </action>
  <verify>
    <automated>cargo test -p nvtx-analyzer 2>&1 | grep -Eq "test result: FAILED|assertion" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p nvtx-analyzer` compiles with the five new tests present.
    - The five tests FAIL (RED), not error on missing symbols.
    - `grep -q "pushpop_nested_per_thread" integrations/nvtx/analyzer/tests/pushpop.rs`.
  </acceptance_criteria>
  <done>Five per-thread Push/Pop + tolerance + malformed-completion tests exist and fail on assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-(thread_id, domain) push/pop stacks + synthetic close + orphan skip (GREEN)</name>
  <files>integrations/nvtx/analyzer/src/ranges.rs, integrations/nvtx/analyzer/src/model.rs</files>
  <read_first>
    - integrations/nvtx/analyzer/src/ranges.rs (existing RangeStart/End matching to extend)
    - integrations/nvtx/analyzer/src/model.rs (pass-2 replay loop to add Push/Pop dispatch)
    - integrations/nvtx/injection/src/init.rs (range_push_level/range_pop_level :56-82 — the per-(thread,domain) grain to mirror)
    - .planning/phases/02-nvtx-model-tolerant-analyzer/02-RESEARCH.md (§Range Reconstruction Push/Pop + §Tolerance)
    - .planning/phases/02-nvtx-model-tolerant-analyzer/02-PATTERNS.md (ranges.rs section)
  </read_first>
  <behavior>
    - A RangePush pushes an open span onto the `(thread_id, domain)` stack; the matching RangePop pops the
      innermost open span on that same key, forming an `NvtxSpan { kind: PushPop, thread_id: Some(tid), parent, .. }`.
    - Interleaved threads nest independently; a Pop on thread B never closes thread A's Push.
    - `parent` is the span that is on top of the stack at pop time (None if the popped span was outermost).
    - Unclosed pushes at end-of-trace close at max observed ts with `synthetic_end = true` and `warn!`.
    - Orphan Pop (empty stack for that key) → `warn!` + skip, no span, no panic.
    - The full malformed stream returns `Ok` with no panic.
  </behavior>
  <action>
    In `ranges.rs` add a `HashMap<(u32 thread_id, u64 domain), Vec<OpenPushSpan>>` stack map. On `RangePush`,
    push an open record (domain, thread_id, resolved name via the tables, category/color/payload, start ts).
    On `RangePop { domain, thread_id }`, pop the innermost open record for `(thread_id, domain)`; capture
    `parent` from the new top-of-stack (its `SpanId`) before finalizing; clamp `end = end.max(start)`; emit an
    `NvtxSpan { kind: PushPop, thread_id: Some(thread_id), parent, synthetic_end: false, .. }`. Empty stack →
    `warn!` + skip. After pass 2, drain every remaining open push: close at the max observed timestamp,
    `synthetic_end = true`, `warn!`. Wire `RangePush`/`RangePop` dispatch into the `model.rs` pass-2 loop
    (alongside the existing Start/End dispatch). Assign `SpanId`s so `parent` references resolve. Keep all
    Plan 02/03 behavior intact.
  </action>
  <verify>
    <automated>cargo test -p nvtx-analyzer pushpop_nested_per_thread pushpop_single_thread unclosed_closed_at_trace_end orphan_pop_skipped malformed_stream_completes</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nvtx-analyzer` — all five new tests pass (GREEN); all prior Plan 02/03 tests still pass.
    - `grep -Eq "thread_id.*domain|\\(thread" integrations/nvtx/analyzer/src/ranges.rs` (stack keyed by (thread_id, domain), not global).
    - `grep -c "warn!" integrations/nvtx/analyzer/src/ranges.rs` >= 2 (orphan pop + synthetic close logged).
    - `cargo clippy -p nvtx-analyzer --all-targets --all-features -- -D warnings` clean; `cargo fmt --check` clean.
    - `grep -rc "quent_analyzer\|quent_model\|RtFsm" integrations/nvtx/analyzer/src` returns 0.
  </acceptance_criteria>
  <done>Push/Pop nest correctly per thread with parent capture; unclosed pushes close synthetic, orphan pops skip, and the full malformed stream completes without panic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| captured event stream → per-thread stacks | Untrusted, unbalanced Push/Pop sequences drive stack push/pop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | unbalanced Push/Pop replay | mitigate | Orphan Pop `warn!`+skip; unclosed Push closed at trace-end; pop uses `Vec::pop` (no `unwrap` on empty), never panics. |
| T-02-06 | Denial of Service | pathological stream: billions of never-popped Push grow the stacks unboundedly | accept | Bounded by the finite captured session (same rationale as T-02-02); documented. If streaming ingestion is later added, cap stack depth. |
| T-02-03 | Denial of Service | duration on out-of-order push/pop | mitigate | Clamp `end = end.max(start)` before subtract. |
</threat_model>

<verification>
- `cargo test -p nvtx-analyzer` green (12 tests across Plans 02-04, incl. `malformed_stream_completes`).
- `cargo clippy -p nvtx-analyzer --all-targets --all-features -- -D warnings` clean; `cargo fmt --check` clean.
</verification>

<success_criteria>
- Push/Pop reconstruct as per-`(thread_id, domain)` nested stacks; a Pop matches the most recent Push on the same thread (ANA-03).
- Concurrent interleaved threads nest independently (no cross-thread mismatch).
- Unclosed pushes close at trace-end flagged synthetic; the full malformed stream analyzes to completion with no panic (ANA-05, success criterion 3).
</success_criteria>

<output>
Create `.planning/phases/02-nvtx-model-tolerant-analyzer/02-04-SUMMARY.md` when done.
</output>
