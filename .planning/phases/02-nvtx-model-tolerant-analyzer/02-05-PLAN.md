---
phase: 02-nvtx-model-tolerant-analyzer
plan: 05
type: execute
wave: 5
depends_on: ["02-03", "02-04"]
files_modified:
  - integrations/nvtx/analyzer/src/resource.rs
  - integrations/nvtx/analyzer/src/stats.rs
  - integrations/nvtx/analyzer/src/span.rs
  - integrations/nvtx/analyzer/src/model.rs
  - integrations/nvtx/analyzer/src/lib.rs
  - integrations/nvtx/analyzer/Cargo.toml
  - integrations/nvtx/analyzer/tests/resource.rs
  - integrations/nvtx/analyzer/tests/stats.rs
  - integrations/nvtx/analyzer/tests/roundtrip.rs
autonomous: true
requirements: [ANA-06, MOD-02]

must_haves:
  truths:
    - "Resources reconstruct as named lifespans matched by handle alone (Create -> Destroy)"
    - "identifier_type labels the core/generic NVTX resource types; unknown/extension types pass through raw"
    - "Range statistics compute count and total/avg/min/max duration per (name, domain, category)"
    - "A real nvtx-example capture reconstructs into the expected labeled model end-to-end"
  artifacts:
    - path: "integrations/nvtx/analyzer/src/resource.rs"
      provides: "resource lifespan matching + identifier_type labels"
      contains: "identifier_type"
    - path: "integrations/nvtx/analyzer/src/stats.rs"
      provides: "per-(name,domain,category) aggregation"
      contains: "RangeStats"
  key_links:
    - from: "integrations/nvtx/analyzer/src/resource.rs"
      to: "handle-keyed Create/Destroy matching"
      via: "ResourceDestroy carries only handle (no domain)"
      pattern: "handle"
    - from: "integrations/nvtx/analyzer/src/stats.rs"
      to: "completed spans grouped by (name, domain, category)"
      via: "fold over span set"
      pattern: "name.*domain.*category"
---

<objective>
Complete the NVTX model surface and the analyzer's query layer: model resources as named
lifespans (D-09/D-10/D-11), compute range statistics per `(name, domain, category)` (ANA-06), and
prove the whole core end-to-end against a real `nvtx-example` capture. Resources match
`ResourceCreate → ResourceDestroy` by `handle` ALONE (Pitfall 4 — Destroy carries no domain);
`identifier_type` labels the core/generic NVTX types and passes unknown/CUDA-extension types
through raw (core-now / extension-deferred, D-11). Do NOT fabricate capacity/occupancy semantics
(D-10) — a resource is structurally just another `NvtxSpan { kind: Resource }`.

Purpose: finish MOD-02 (resources) + ANA-06 (statistics) and validate the full slice on real telemetry.
Output: resources + statistics on the model, and a real-capture roundtrip proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-nvtx-model-tolerant-analyzer/02-CONTEXT.md
@.planning/phases/02-nvtx-model-tolerant-analyzer/02-RESEARCH.md
@.planning/phases/02-nvtx-model-tolerant-analyzer/02-PATTERNS.md
@.planning/phases/02-nvtx-model-tolerant-analyzer/02-03-SUMMARY.md
@.planning/phases/02-nvtx-model-tolerant-analyzer/02-04-SUMMARY.md

<interfaces>
<!-- Extracted from source; do not re-explore. -->

From integrations/nvtx/events/src/lib.rs — resource variants:
  ResourceCreate { domain: u64, handle: u64, identifier_type: i32, identifier: u64, message: Option<NvtxMessage> }
  ResourceDestroy { handle: u64 }   // NO domain — match by handle alone; recover domain from the Create

From integrations/nvtx/analyzer/src/span.rs / tables.rs (Plans 02-03):
  NvtxSpan { .., name, category: Option<u32>, start, end, kind: SpanKind, synthetic_end }
  SpanKind::Resource
  ResolutionTables::resolve_message(domain, &Option<NvtxMessage>) -> String   // resolve resource name

Happy-path capture (integrations/nvtx/example) — the roundtrip fixture source:
  nvtx_example::run_capture(session: Uuid, exporter: EventCallback) -> Result<(), Box<dyn Error>>
  EventCallback downcasts recorded.event to Event<NvtxEventEntity> (id + timestamp + data);
  reconstruct Event::new(id, timestamp, data.clone()) to feed the builder.
  annotated_work emits: NameThread, Mark("startup"), RangePush/Pop("phase-1"), RangeStart/End("phase-2").
  dev-deps to add (mirror integrations/nvtx/example/Cargo.toml): nvtx-example, quent-instrumentation (feature "io-callback"), uuid.

OQ#3 — core identifier_type values are [ASSUMED]: confirm against the vendored NVTX headers used by
integrations/nvtx/injection/build.rs (pixi nvtx-c: nvtxResourceGenericType_t / nvtxResourceCUDAType_t).
Label the generic/core set; pass unknown through as "<identifier_type {n}>".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resource lifespans by handle + identifier_type labels (ANA-06 model surface, D-09/10/11)</name>
  <files>integrations/nvtx/analyzer/src/resource.rs, integrations/nvtx/analyzer/src/span.rs, integrations/nvtx/analyzer/src/model.rs, integrations/nvtx/analyzer/src/lib.rs, integrations/nvtx/analyzer/tests/resource.rs</files>
  <read_first>
    - integrations/nvtx/events/src/lib.rs (ResourceCreate :108-120, ResourceDestroy :121-125)
    - integrations/nvtx/injection/src/convert.rs (read_resource :459-494 — field semantics)
    - integrations/nvtx/injection/build.rs (bindgen allowlist — where nvtx-c headers/resource types come from)
    - integrations/nvtx/analyzer/src/tables.rs (resolve_message) + span.rs (NvtxSpan/SpanKind::Resource)
    - integrations/nvtx/analyzer/src/model.rs (pass-2 replay loop)
    - .planning/phases/02-nvtx-model-tolerant-analyzer/02-RESEARCH.md (§Model Representation Resource modeling :336-349; OQ#3)
    - .planning/phases/02-nvtx-model-tolerant-analyzer/02-PATTERNS.md (resource.rs section + Pitfall 4)
  </read_first>
  <behavior>
    - `resource_lifespan`: ResourceCreate(handle=H, domain=D, identifier_type=core, message="buf") + ResourceDestroy(handle=H) → one `NvtxSpan { kind: Resource, domain: D, name: "buf", start, end }`, matched by handle alone; domain recovered from the Create.
    - A core/generic `identifier_type` yields a static label; an unknown/extension value yields exactly "<identifier_type {n}>".
    - A ResourceCreate with no Destroy closes at trace-end, `synthetic_end = true`.
    - An orphan ResourceDestroy (no open Create) is skipped with `warn!`, no panic.
  </behavior>
  <action>
    First, confirm the core `identifier_type` enum values: read the NVTX resource-type enums the injection
    bindgen exposes (nvtx-c headers via `integrations/nvtx/injection/build.rs`; run under `pixi run` if
    needed to locate them). Enumerate the generic/core `nvtxResourceGenericType_t` set and map each to a
    static `&str` label; if a value cannot be confirmed, treat it as unknown. Create `resource.rs`: a
    `HashMap<u64 handle, OpenResource>` matched by handle ALONE; on `ResourceCreate` insert (domain,
    identifier_type, identifier, resolved name via `resolve_message(domain, &message)`, start ts); on
    `ResourceDestroy { handle }` remove + form an `NvtxSpan { kind: Resource, .. }` (clamp end); unclosed →
    synthetic close at trace-end; orphan destroy → `warn!` + skip. Add a `fn label_identifier_type(i32) ->
    String` with the confirmed core `match` + pass-through `"<identifier_type {n}>"`. In `span.rs`, add an
    `identifier_type_label: Option<String>` (or a small `NvtxResource` view over the span) so the label is
    queryable. Wire ResourceCreate/Destroy dispatch into `model.rs` pass 2; expose `model.resources()`.
    Add `tests/resource.rs` (`resource_lifespan`, plus the label/unclosed/orphan cases). Do NOT fabricate
    capacity/occupancy (D-10).
  </action>
  <verify>
    <automated>cargo test -p nvtx-analyzer resource_lifespan</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nvtx-analyzer resource_lifespan` (and label/unclosed/orphan cases) pass.
    - `grep -Eq "handle" integrations/nvtx/analyzer/src/resource.rs` and the match key is `handle` alone (NOT `(domain, handle)`).
    - `grep -q "identifier_type {" integrations/nvtx/analyzer/src/resource.rs` (unknown-type pass-through format present).
    - `grep -rc "occupancy\|capacity\|utilization" integrations/nvtx/analyzer/src` returns 0 (D-10 — no fabricated semantics).
    - `cargo clippy -p nvtx-analyzer --all-targets --all-features -- -D warnings` clean; `cargo fmt --check` clean.
  </acceptance_criteria>
  <done>Resources reconstruct as handle-matched named lifespans with core identifier_type labels; unknown types pass through; no fabricated capacity semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Range statistics per (name, domain, category) (ANA-06)</name>
  <files>integrations/nvtx/analyzer/src/stats.rs, integrations/nvtx/analyzer/src/model.rs, integrations/nvtx/analyzer/src/lib.rs, integrations/nvtx/analyzer/tests/stats.rs</files>
  <read_first>
    - integrations/nvtx/analyzer/src/span.rs (NvtxSpan fields — name/domain/category/start/end/kind/synthetic_end)
    - integrations/nvtx/analyzer/src/model.rs (span accessors)
    - .planning/phases/02-nvtx-model-tolerant-analyzer/02-RESEARCH.md (§Range Statistics :388-398; OQ#2 synthetic contribution)
    - .planning/phases/02-nvtx-model-tolerant-analyzer/02-PATTERNS.md (stats.rs section)
  </read_first>
  <behavior>
    - `range_statistics`: given several completed PushPop/StartEnd spans, group by `(resolved name, domain, category)`; per group compute `count`, `total_duration`, `avg = total/count`, `min`, `max` in ns.
    - Only range spans participate: marks (instants) are excluded; resources are excluded from range stats (reported separately or omitted).
    - `avg` guards `count == 0`; zero-duration spans contribute `0`; synthetic-closed spans are included but their synthetic count is separately tracked per group (OQ#2).
  </behavior>
  <action>
    Create `stats.rs`: a `struct RangeStats { count, total_duration, avg_duration, min_duration,
    max_duration, synthetic_count }` and a `struct StatsKey { name: String, domain: u64, category:
    Option<u32> }`. Implement a fold over the model's completed range spans (kind PushPop | StartEnd only)
    producing a `Vec<RangeStats>` or `HashMap<StatsKey, RangeStats>`, `duration = end - start` (already
    clamped ≥ 0 upstream), `avg = total / count` with a `count == 0` guard, tracking a `synthetic_count`
    for spans with `synthetic_end`. Expose via `model.range_statistics()`. Add `tests/stats.rs`
    (`range_statistics`) asserting count/total/avg/min/max for a known group and that marks/resources are excluded.
  </action>
  <verify>
    <automated>cargo test -p nvtx-analyzer range_statistics</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nvtx-analyzer range_statistics` passes.
    - `grep -q "RangeStats" integrations/nvtx/analyzer/src/stats.rs` and `grep -Eq "min|max|avg|total" integrations/nvtx/analyzer/src/stats.rs`.
    - `grep -q "range_statistics" integrations/nvtx/analyzer/src/model.rs` (query accessor exposed).
    - `cargo clippy -p nvtx-analyzer --all-targets --all-features -- -D warnings` clean; `cargo fmt --check` clean.
  </acceptance_criteria>
  <done>Statistics compute count and total/avg/min/max per (name, domain, category) over range spans, excluding marks/resources.</done>
</task>

<task type="auto">
  <name>Task 3: Real nvtx-example capture roundtrip (happy path, feature-gated)</name>
  <files>integrations/nvtx/analyzer/Cargo.toml, integrations/nvtx/analyzer/tests/roundtrip.rs</files>
  <read_first>
    - integrations/nvtx/example/tests/capture.rs (the collecting-EventCallback pattern :36-68)
    - integrations/nvtx/example/src/lib.rs (run_capture :25-41, annotated_work :45-54 — what labels to expect)
    - integrations/nvtx/example/Cargo.toml (dev-dep shape for nvtx-example + quent-instrumentation io-callback + uuid)
    - integrations/nvtx/analyzer/src/model.rs (the public build + query surface to assert against)
  </read_first>
  <action>
    Add an optional `real-capture-tests` feature to `integrations/nvtx/analyzer/Cargo.toml` that activates
    dev-deps `nvtx-example`, `quent-instrumentation` (feature `io-callback`), and `uuid` — keeping the pure
    unit-test build pixi/bindgen-free (the roundtrip pulls in the injection cdylib's bindgen). Add
    `tests/roundtrip.rs` gated `#![cfg(feature = "real-capture-tests")]` with `example_capture_roundtrip`:
    run `nvtx_example::run_capture(Uuid::now_v7(), sink)` collecting full `Event<NvtxEventEntity>` (clone
    id+timestamp+data), feed them into `NvtxModelBuilder::build`, and assert the reconstructed model
    contains: a NameThread-named thread, a "startup" mark, a "phase-1" PushPop span with a real thread_id,
    and a "phase-2" StartEnd span; and that `range_statistics()` reports the two ranges. Because injection
    runs process-global one-shot, keep it a single-threaded single test (no parallel capture).
  </action>
  <verify>
    <automated>pixi run cargo test -p nvtx-analyzer --features real-capture-tests example_capture_roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - `pixi run cargo test -p nvtx-analyzer --features real-capture-tests example_capture_roundtrip` passes.
    - Plain `cargo test -p nvtx-analyzer` (no feature) still builds/passes WITHOUT requiring nvtx-c/bindgen (roundtrip test compiled out).
    - `grep -q "real-capture-tests" integrations/nvtx/analyzer/Cargo.toml`.
    - `pixi run cargo clippy -p nvtx-analyzer --all-targets --all-features -- -D warnings` clean; `pixi run cargo fmt --check` clean.
  </acceptance_criteria>
  <done>A real nvtx-example capture reconstructs into the expected labeled model + statistics; the heavy dependency is feature-gated so unit tests stay light.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| captured event stream → resource/stats layers | Untrusted resource handles, identifier_types, and durations drive matching and arithmetic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | orphan ResourceDestroy / unclosed ResourceCreate | mitigate | Orphan destroy `warn!`+skip; unclosed create closed at trace-end; `HashMap::remove` (no `unwrap`), never panics. |
| T-02-03 | Denial of Service | statistics `total_duration` accumulation + `avg` division | mitigate | Durations are clamped ≥ 0 upstream; use saturating add for `total`; guard `count == 0` before dividing for `avg`. |
| T-02-07 | Tampering | unrecognized/extension identifier_type | mitigate | Unknown types pass through as quoted `"<identifier_type {n}>"` — never indexed/dereferenced, no fabricated semantics (D-10). |
</threat_model>

<verification>
- `cargo test -p nvtx-analyzer` green (unit tests, no feature).
- `pixi run cargo test -p nvtx-analyzer --features real-capture-tests` green (adds roundtrip).
- `pixi run cargo clippy -p nvtx-analyzer --all-targets --all-features -- -D warnings` clean; `pixi run cargo fmt --check` clean.
</verification>

<success_criteria>
- Resources reconstruct as handle-matched named lifespans with core identifier_type labels; unknown types pass through raw (MOD-02, D-09/10/11).
- Range statistics compute count and total/avg/min/max per `(name, domain, category)` (ANA-06).
- A real nvtx-example capture reconstructs end-to-end into the expected labeled model.
</success_criteria>

<output>
Create `.planning/phases/02-nvtx-model-tolerant-analyzer/02-05-SUMMARY.md` when done.
</output>
