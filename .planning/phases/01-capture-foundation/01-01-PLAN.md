---
phase: 01-capture-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - integrations/nvtx/events/Cargo.toml
  - integrations/nvtx/events/src/lib.rs
  - integrations/nvtx/events/src/attributes.rs
  - integrations/nvtx/events/src/payload.rs
  - Cargo.toml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CAP-02, CAP-03]
must_haves:
  truths:
    - "NvtxEvent covers every core NVTX call kind (push/pop, RangeStart/End, mark, domain create/destroy, registered string, category naming, thread naming, resource create/destroy) as verbatim variants with raw u64 handles"
    - "A captured NvtxEvent round-trips losslessly through serde_json (ndjson vocabulary), including the CORE nvtxEventAttributes payload union"
    - "REQUIREMENTS.md CAP-03 and ROADMAP.md Phase 1 success-criterion 2 state that the CORE payload union is captured now and the payload-extension module is deferred (D-12)"
  artifacts:
    - path: "integrations/nvtx/events/src/lib.rs"
      provides: "NvtxEvent enum, NvtxMessage, NvtxEventKind + EntityEvent impl"
      contains: "enum NvtxEvent"
    - path: "integrations/nvtx/events/src/payload.rs"
      provides: "CORE payload union type + deferred payload-extension event types (defined, not wired)"
      contains: "payload"
    - path: "integrations/nvtx/events/Cargo.toml"
      provides: "quent-nvtx-events package, zero Quent-internal deps beyond serde/uuid"
      contains: "name = \"quent-nvtx-events\""
  key_links:
    - from: "Cargo.toml"
      to: "integrations/nvtx/events"
      via: "workspace members + default-members registration"
      pattern: "integrations/nvtx/events"
    - from: "integrations/nvtx/events/src/lib.rs"
      to: "quent EntityEvent contract"
      via: "impl EntityEvent for NvtxEventKind"
      pattern: "impl EntityEvent"
---

<objective>
Establish `quent-nvtx-events`: the verbatim, Quent-agnostic NVTX event vocabulary
every downstream crate speaks. This is the shared contract (D-01/D-02) — the
injection cdylib produces it and the bridge wraps it in `Event<T>`. Also re-scope
the payload requirement per D-12 so CAP-03 stays covered but honest.

Purpose: A stable, serde round-trippable `NvtxEvent` vocabulary that Phase 2's
analyzer will consume unchanged, with raw handles (no capture-time resolution).
Output: `integrations/nvtx/events/` crate + workspace registration + D-12 doc edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-foundation/01-CONTEXT.md
@.planning/phases/01-capture-foundation/01-RESEARCH.md
@.planning/phases/01-capture-foundation/01-PATTERNS.md

<interfaces>
<!-- Analog contracts the executor mirrors. No codebase exploration needed. -->
From crates/events/src/trace.rs (enum-of-structs serde vocabulary, lines 10-55):
  - `pub type SpanId = u64;`
  - `#[derive(Debug, Deserialize, Serialize)] pub struct SpanInit { ... }`
  - `#[derive(Debug, Deserialize, Serialize)] pub enum TraceEvent { Init(..), Span(..), .. }`
From crates/events/src/lib.rs (lines 13-16):
  - `pub trait EntityEvent { const NAME: &'static str; }`
  - `Event<T>` wrapper + `Event::new_now(id, data)` (lines 38-45) — used by the bridge in plan 03, NOT here.
From crates/events/Cargo.toml (lines 1-13): `.workspace = true` inheritance for version/edition/publish; `serde.workspace = true`, `uuid = { workspace = true, features = ["serde"] }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create quent-nvtx-events vocabulary crate and register it in the workspace</name>
  <files>integrations/nvtx/events/Cargo.toml, integrations/nvtx/events/src/lib.rs, integrations/nvtx/events/src/attributes.rs, integrations/nvtx/events/src/payload.rs, Cargo.toml</files>
  <read_first>
    - crates/events/src/trace.rs (lines 1-55) — SPDX header + enum-of-structs serde vocabulary shape to mirror.
    - crates/events/src/lib.rs (lines 4, 13-16) — crate-level `//!` doc, small-lib re-export, `EntityEvent` trait.
    - crates/events/Cargo.toml (lines 1-13) — `.workspace = true` inheritance pattern.
    - root Cargo.toml (lines 3-63 members, 65-104 default-members, 106-135 workspace.dependencies) — registration + the tests/fixed default-members exclusion comment style.
    - .planning/phases/01-capture-foundation/01-PATTERNS.md §"integrations/nvtx/events" — exact analog assignments and NvtxMessage/handle shape.
    - PR #87 (reference-only, D-01): `integrations/nvtx/events/src/{lib,attributes,payload}.rs` — the NvtxEvent vocabulary shape.
  </read_first>
  <behavior>
    - Test 1: each core `NvtxEvent` variant (RangePush, RangePop, RangeStart, RangeEnd, Mark, DomainCreate, DomainDestroy, RegisterString, NameCategory, NameThread, ResourceCreate, ResourceDestroy) serializes then deserializes back equal via `serde_json`.
    - Test 2: a Mark carrying the CORE payload union (scalar u64/i64/f64/pointer value + payloadType tag) round-trips with the payload value preserved verbatim.
    - Test 3: `NvtxMessage::String(..)` and `NvtxMessage::RegisteredHandle(u64)` both round-trip (registered messages keep only the raw handle, never resolved).
  </behavior>
  <action>
    Create the `quent-nvtx-events` package under `integrations/nvtx/events/`. SPDX header
    (`SPDX-License-Identifier: Apache-2.0`, 2026 NVIDIA copyright) + crate-level `//!` doc on
    every `.rs`. `lib.rs` declares private modules `attributes`, `payload` and re-exports the
    public surface (`NvtxEvent`, `NvtxMessage`, `NvtxEventKind`). Define `NvtxEvent` as an enum
    of structs mirroring `crates/events/src/trace.rs`: verbatim variants for every core kind in
    ROADMAP success-criterion 1 with raw `u64` handles (domain/category/resource/registeredString
    ids) and no name resolution (D-01, verbatim capture). `NvtxMessage::{String(String),
    RegisteredHandle(u64)}`. In `attributes.rs` model the captured subset of `nvtxEventAttributes`
    (category, color, message, payload). In `payload.rs` define the CORE payload union type
    (scalar value + `payloadType` tag, per D-12) AND define — but do NOT wire — the
    payload-EXTENSION event types (schema register, enum register, binary payload blob) as future
    vocabulary so the stream can carry them later without a vocabulary change (D-12; PR #87 modeled
    these). Add `NvtxEventKind` (the `Event<T>` payload type) and `impl EntityEvent for
    NvtxEventKind { const NAME: &'static str = "NvtxEvent"; }`. Cargo.toml uses `.workspace = true`
    for version/edition/publish and depends ONLY on `serde.workspace = true` and
    `uuid = { workspace = true, features = ["serde"] }` — no Quent-internal deps (D-03; keep
    `quent-time`/`quent-attributes` OUT — the CORE payload union is plain scalars). Register
    `integrations/nvtx/events` in root Cargo.toml `members` (new `# NVTX integration crates`
    comment block) and `default-members`. Put the three `#[cfg(test)]` serde round-trip tests in
    `lib.rs`/`payload.rs`.
  </action>
  <verify>
    <automated>pixi run cargo test -p quent-nvtx-events</automated>
  </verify>
  <acceptance_criteria>
    - `pixi run cargo test -p quent-nvtx-events` passes (all serde round-trip tests green).
    - `pixi run cargo clippy -p quent-nvtx-events --all-targets --all-features --locked -- -D warnings` is clean.
    - `pixi run cargo fmt --all -- --check` is clean.
    - `grep -v '^//' integrations/nvtx/events/src/lib.rs | grep -c "impl EntityEvent"` returns ≥ 1.
    - `integrations/nvtx/events/Cargo.toml` contains no `quent-instrumentation`/`quent-events` dependency (grep returns nothing — D-03 separability).
    - Every new `.rs` file's first two lines are the SPDX copyright + license header.
  </acceptance_criteria>
  <done>The vocabulary crate builds, its serde round-trip tests pass, it is registered in the workspace, and it carries zero Quent-internal dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Re-scope CAP-03 and Phase 1 success-criterion 2 to core-union-now / extension-deferred (D-12)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 14, CAP-03) — current wording.
    - .planning/ROADMAP.md (lines 42-43, Phase 1 success-criterion 2) — current wording.
    - .planning/phases/01-capture-foundation/01-CONTEXT.md D-12 (lines 114-125) — the mandated narrowing and rationale (libcudf emits zero payload-extension events).
  </read_first>
  <action>
    Edit CAP-03 in REQUIREMENTS.md to state that the CORE `nvtxEventAttributes` payload UNION is
    captured verbatim (undecoded) in Phase 1, and that the payload-EXTENSION module (schema
    registration, enum registration, binary `nvtxPayloadData_t`) is DEFERRED to a later phase
    (natural home alongside PAY-01 decode). Edit ROADMAP.md §"Phase 1: Capture Foundation"
    success-criterion 2 identically. Do not change any other requirement or criterion. Reference
    D-12 in an inline note so traceability is preserved. Keep CAP-03 mapped to Phase 1 (still
    COVERED, just re-scoped).
  </action>
  <verify>
    <automated>grep -i "extension" /home/pgarigipati/gitroot/quent-workspace/worktrees/nvtx_consumer/.planning/REQUIREMENTS.md | grep -ci "defer"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md CAP-03 mentions the CORE payload union captured now AND the payload-extension module deferred.
    - ROADMAP.md Phase 1 success-criterion 2 matches the same narrowed scope.
    - CAP-03 still appears in the Phase 1 traceability row (grep `CAP-03 | Phase 1`).
    - No other REQ-ID wording changed (git diff limited to the CAP-03 line and SC-2 lines).
  </acceptance_criteria>
  <done>Both planning docs reflect the narrowed payload scope and CAP-03 remains mapped to Phase 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ndjson file → deserializer | Captured events are read back from a file in tests; treated as data, not code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | serde deserialization of NvtxEvent from ndjson | accept | Test-only fixtures; no untrusted network input at this tier. Deny unknown fields is unnecessary for a lossless verbatim vocabulary. |
| T-01-02 | Information disclosure | raw u64 handles in the vocabulary | accept | Handles are opaque ids, not secrets; verbatim by design (resolution is Phase 2). |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new external packages introduced in this plan (serde/uuid already workspace deps). No install checkpoint required. |
</threat_model>

<verification>
- `pixi run cargo test -p quent-nvtx-events` green.
- `pixi run cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` clean.
- REQUIREMENTS.md / ROADMAP.md reflect D-12 narrowing; CAP-03 still Phase-1 mapped.
</verification>

<success_criteria>
The verbatim NVTX event vocabulary exists, round-trips through serde, is registered in the
workspace with zero Quent-internal deps, and the payload requirement is honestly re-scoped (D-12).
</success_criteria>

<output>
Create `.planning/phases/01-capture-foundation/01-01-SUMMARY.md` when done.
</output>
