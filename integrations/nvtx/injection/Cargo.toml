[package]
name = "quent-nvtx-injection"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
quent-nvtx-events = { path = "../events" }
libc = "0.2"

[build-dependencies]
bindgen = "0.72"
cc = "1.2"
cargo_metadata = "0.19"

[dev-dependencies]
nvidia-nvtx = { git = "https://github.com/NVIDIA/NVTX", branch = "release-v3", default-features = false, features = [] }
