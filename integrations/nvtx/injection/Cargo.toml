[package]
name = "quent-nvtx-injection"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
quent-nvtx-events = { path = "events" }
libc = "0.2"

[build-dependencies]
bindgen = "0.71"
cc = "1.2"

[dev-dependencies]
nvidia-nvtx = { path = "../vendor/nvtx-repo/rust", default-features = false, features = [] }
