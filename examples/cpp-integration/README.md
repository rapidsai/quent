# C++ Integration Example

## Build

Requires: cmake, a C++ compiler, and Rust toolchain (via pixi or manually).

```bash
pixi shell
cd examples/cpp-integration/cpp
cmake -B build
cmake --build build
```

## Run

```bash
./build/quent_cpp_example
```
