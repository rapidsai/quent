# Resource Group

A Resource Group is a [Resource][resource] that represents a hierarchical
grouping over a set of Resources.

FSM:

```text
⊙ -> initializing -> operating -> finalizing -> ⊗
```

> TODO: figure out whether we really need this. It does help a lot to group specific things together in the UI, but should it necessarily be an FSM?

[resource]: ./resource.md
