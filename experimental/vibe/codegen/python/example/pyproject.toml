[build-system]
requires = ["maturin>=1.10,<2"]
build-backend = "maturin"

[project]
name = "quent-demo"
version = "0.0.0"
requires-python = ">=3.11"

[tool.maturin]
manifest-path = "bridge/Cargo.toml"
module-name = "quent_demo"
include = [
  { path = "quent_demo/__init__.pyi", from = "out-dir", to = "" },
  { path = "quent_demo/py.typed", from = "out-dir", to = "" },
]
