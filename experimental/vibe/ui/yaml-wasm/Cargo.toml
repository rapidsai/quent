[package]
name = "quent-schema-explorer-yaml"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
quent-schema = { path = "../../../../crates/schema", features = ["serde"] }
quent-yaml = { path = "../../../../crates/yaml" }
serde_json = "1.0.145"
wasm-bindgen = "=0.2.126"

[workspace]
